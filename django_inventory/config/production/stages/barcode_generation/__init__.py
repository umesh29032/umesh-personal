"""Barcode Generation stage package (M2.5 — adapter-only).

Holds the handler (thin adapter over barcode_generation_service). Code moves here
only after dispatch is migrated to the registry and proven (post-M2.6).
"""
