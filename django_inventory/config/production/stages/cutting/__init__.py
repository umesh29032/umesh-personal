"""Cutting stage package (M2.4 — adapter-only).

Holds the handler (thin adapter over cutting_service). The 1189-line service splits
into bundle/breakup/completion modules here only after dispatch is migrated and
proven (post-M2.6).
"""
