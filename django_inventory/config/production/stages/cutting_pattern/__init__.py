"""Pattern Design stage package (code: cutting_pattern; M2.3 — adapter-only).

Holds the handler (thin adapter over cutting_pattern_service). Service/models/views
move here only after dispatch is migrated to the registry and proven (post-M2.6).
"""
