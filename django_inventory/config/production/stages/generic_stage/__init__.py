"""generic_stage — THE config-only archetype (R10-B, frozen architecture).

One handler serves EVERY stage (manual or machine) that has no bespoke
package: the registry falls back here, so a new operation = a Stage row +
Access-hub skills + Flow-Editor rate. Zero code per stage — frozen rule 11.
"""
