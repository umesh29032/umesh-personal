---
id: deployment-backlog
type: status-anchor
status: active
owner: handwritten
scope: all — navigation/state
anchors: —
verified: 2026-07-13
---

# Deployment Backlog — non-blocking polish found during V1.1 sprint

> Seeded 2026-07-12 (V1.1 Item 3). Rule: record what a sprint item FINDS but
> must not BUILD (scope discipline). Each row: where found · what · why not now.
> Owner reviews before deploy; none of these block the freeze.
> **Scope boundary (2026-07-13, Q-A6):** this file = CAMPAIGN findings/INFO rows.
> Engineering/product open items (S6, R11, deferred features) live in
> [PENDING_BACKLOG.md](PENDING_BACKLOG.md). Two registers, two scopes.

| # | Found during | Item | Why deferred |
|---|---|---|---|
| 1 | V1.1-3 exports | `_next_export_code()` sequencer is count+parse inside `@transaction.atomic`, not contention-safe at high throughput (documented in code). Fine at factory scale (few exports/day); future = per-year PG sequence. | Non-permission; pre-existing documented trade-off |
| 2 | V1.1-3 exports | ~~Barcode dashboard (`/tracking/`) row-action links (`View`/`Print QR`) rendered 0 rows for the dev worker while the page itself returned 200 — worker sees an empty dashboard table.~~ **RESOLVED during Worker Certification Phase C (2026-07-12): no longer an open concern.** Current middleware + SidebarAccess DB policy (accounts/0018 stripped `worker` from the `tracking:dashboard` rule) blocks workers BEFORE the dashboard renders — browser-proven as `dev.ow.a`: `GET /tracking/` → 302 to My-Dashboard + "You don't have the access to this page." The original 200-with-empty-table observation is stale and retained here only as historical context; the "should workers see the dashboard at all" question is answered by owner-approved 0018 (they don't). Evidence: [WORKER_ROLE_CERTIFICATION.md](WORKER_ROLE_CERTIFICATION.md) Phase C. | Resolved — Phase C certification; row preserved for history |
| 3 | V1.1-3 exports | `export_list.html` inline `style="padding:4px 10px; font-size:12px;"` on the Download button — tokens-only rule candidate next time that template is touched. | Pure polish; page is now management-only |
| 4 | V1.1-3 battery | **patterns_ai tests are NOT `--parallel`-safe**: standalone `manage.py test patterns_ai --parallel 4` flakes with a DIFFERENT failure set each run (block3a/3c orphan sweeps, p2/p3/m8 "unreadable image" in per-module `tempfile.mkdtemp` MEDIA dirs); sequential run = green 528/528. Battery is canonical SEQUENTIAL. Future fix = per-test-class media isolation (setUpClass mkdtemp + addClassCleanup) in patterns_ai tests. **MGT-F addendum (2026-07-12): battery is also canonical FRESH-DB — never `--keepdb` across suites.** A `--keepdb` patterns_ai run after the 9-app suite errored ×2 (`Role.DoesNotExist`): `TransactionTestCase` classes (raw_materials/production) truncate migration-seeded Role rows without re-running data migrations, and keepdb carries the emptied table into the next test process. Fresh-DB rerun green 528/528 (no product-code cause). | Test-infra only, zero product code; sequential fresh-DB battery green; fixing = patterns_ai test refactor outside V1.1-3 scope |
| 5 | Worker-cert Phase E (raw_materials) | ~~**Master delete-confirm GET = 500 NoReverseMatch for ANY allowed role**: `master_confirm_delete.html:24` does `{% url list_url_name %}` but `_MasterDeleteView` (Django `DeleteView`) never puts `list_url_name` in the template context.~~ **FIXED during Management Certification MGT-E (2026-07-12):** re-reproduced pre-fix as manager AND super-admin on all THREE masters (500 ×6, exact `NoReverseMatch: Reverse for '' not found`); fix = `get_context_data` on `_MasterDeleteView` supplying `title` + `list_url_name` (mirrors the create/update/archive views); post-fix 200 ×6 + full browser create→confirm→delete workflow as `dev.mgr`; 2 regression pins in `test_master_gates.py`. Evidence: [MANAGEMENT_ROLE_CERTIFICATION.md](MANAGEMENT_ROLE_CERTIFICATION.md) MGT-E | Resolved — MGT-E certification; row preserved for history |
| ~~6~~ | Management-cert MGT-A (production) | ~~**DOC DRIFT: `docs/production/RBAC.md` "What each role can do" table is stale**~~ **✅ STRUCK 2026-07-13 — FIXED at Phase-7 DOCCLEAN-A (Q-A8):** table rebuilt from the certified gate matrix (MGT-A/E + worker-cert final meta-audit + OFF-A add-on model); row retained per history convention. Original: table said manager Product CRUD "yes" + worker roll-bulk-add "yes" vs SuperAdminOnly code gates (master-data lockdown). | Doc-only, zero code; evidence = [DOCUMENT_CLEANUP_LOG.md](DOCUMENT_CLEANUP_LOG.md) Q-A8 block; cert source = MANAGEMENT_ROLE_CERTIFICATION.md MGT-A |
| 7 | Management-cert MGT-E (raw_materials) | **Master archive-confirm GET with a nonexistent pk = 500** (`_MasterArchiveView.get` does `self.model.objects.get(pk=pk)` — uncaught `DoesNotExist`; probe: `/raw-materials/cloth-types/99999/archive/` → 500 as manager). NOT UI-reachable (list pages only link real pks; edit/delete siblings already 404 via `get_object_or_404`/`DeleteView`). Fails closed: no write, no leak, unbranded error page only. Fix when touched = `get_object_or_404` in `_MasterArchiveView.get`/`post`. **OWN-E owner-relevance check (2026-07-13): stays INFO** — as super-admin the same 500 reproduces on ALL THREE masters (cloth-types/cloth-colors/storage-locations 99999; broadened vs the MGT-E single-master repro), edit/delete siblings 404, and an href census on the live list page proved archive links render ONLY real pks → not owner-UI-reachable either; fails closed. Evidence: [OWNER_VISIBILITY_CERTIFICATION.md](OWNER_VISIBILITY_CERTIFICATION.md) OWN-E | Hand-typed-URL edge only, zero workflow impact; out of MGT-E fix mandate (fix-only-the-confirmed-#5 rule); same-class precedent: MGT-A/D INFO handling |
| 8 | Management-cert MGT-F (machines) | **`MachineAssignView` worker param not validated server-side** (carry-over "assign accepts any user pk", judged with evidence 2026-07-12). Two facets, same locus: (a) POST accepts ANY existing user pk as holder — probes proved assignments created to a listing_team user AND an INACTIVE user (both rollback-wrapped); the UI picker only offers active production-role users, so these need a hand-crafted POST by a management user. (b) non-int `worker`/`adda` in a hand-crafted POST (`worker=''`/`'abc'`) = 500 (uncaught `ValueError` in `get_object_or_404`; nonexistent int pk correctly 404s). No money (grep-pinned zero-₹ app), no permission consequence (possession row grants nothing), fails closed on garbage — data-hygiene only. Fix when touched = re-validate worker against the picker queryset (active + PRODUCTION_ROLES) + int-parse both params. Note: picker DOES include managers/SA by design (PRODUCTION_ROLES role-based picker, machines README). **OWN-F owner-relevance note (2026-07-13): stays INFO** — as super-admin the same hand-crafted `worker=abc` POST reproduces the 500 (uncaught ValueError, zero rows); no new owner-workflow facet; normal SA assign/release lifecycle fully functional (OWN-F evidence). | Management-only surface, not normal-UI-reachable, zero workflow impact; INFO by MGT-F scope rule (fix ONLY confirmed bugs); evidence: MANAGEMENT_ROLE_CERTIFICATION.md MGT-F |
| 9 | Management-cert MGT-G (patterns_ai) | **Negative-transition actor stamps missing on two knowledge tables** (found while certifying cross-manager isolation 2026-07-12): (a) `MarkerUsage` void records `voided_at` + mandatory `void_reason` but has NO `voided_by` column — the acting user survives only as an app-log line (`usage.void <pk> by=<user>` in `marker_feedback_service.void_usage`); (b) `calibration_service.retire_mat` records `status=retired` + mandatory `status_reason` but captures the acting user NOWHERE (no column, no log line). Both paths are management-gated in the service (`_ensure_management`/`_gate`), F2 reason-mandatory, append-only — NOT exploitable; pure audit-hygiene (who voided/retired is unrecoverable later). Commission path already stamps `checked_by` on the check row. Fix when touched = additive `voided_by`/`retired_by` FK columns + stamp in the two services (data/history principles alignment). | Zero permission/money impact, no workflow breakage; out of MGT-G fix mandate (0 confirmed bugs; fix-only-confirmed rule); evidence: MANAGEMENT_ROLE_CERTIFICATION.md MGT-G |
| 10 | Owner-cert OWN-B (production) | **Review-reports "no changes" detection compares strings, not values** (`AddaReportReviewView.post`, worker_report_views.py:347: `str(old_val ...) == (raw or '')`). POSTing a value equal to the stored correction but formatted differently (`29` vs stored `29.00`) re-invokes `set_verified_quantity` and flashes "1 quantity correction(s) saved." even though nothing changed; posting the exact stored format (`29.00`) correctly flashes "No changes.". **Proven inert 2026-07-13 (rollback-wrapped, SA driving):** verified value byte-identical after double re-save AND `VERIFIED_QTY_CORRECTED` AddaHistory count unchanged (4→4 — the service layer stays honest; only the view's changed-counter/flash is wrong). Message-honesty cosmetic only; no money impact (settlement reads the same unchanged value). Fix when touched = compare parsed `Decimal` values, not string equality. | Zero data/audit/money impact (proven); management-gated surface; out of OWN-B fix mandate (0 confirmed bugs; fix-only-confirmed rule); evidence: OWNER_VISIBILITY_CERTIFICATION.md OWN-B |
| 11 | Owner-cert OWN-C (production/a360) | **A360 reconciliation badge collapses multi-lane stages** ([a360.py:200-201](../config/production/views/a360.py#L200-L201): `recon_by_stage = {r['stage']: r['flag'] …}` — dict keyed by stage CODE, so on a multi-lane Adda the N per-lane reconciliation rows collapse to whichever iterates last; the displayed flag badge can show the clean lane's flag while another lane is `over_allocated`). Same root class as the OWN-C-fixed evidence-FK bug (that one PERSISTED wrong audit FKs and was fixed + pinned; this one is a read-only display badge). `reconcile_stage_pay` rows now carry `stage_record_id` (OWN-C fix), so the fix-when-touched is mechanical: aggregate flags per stage across lanes (e.g. worst-flag-wins) or badge per lane. | Display-only (no persisted data, no money math — a360 cost math reads per-SR querysets separately); out of OWN-C smallest-change fix mandate (confirmed defect = the persisted evidence FK, fixed); evidence: OWNER_VISIBILITY_CERTIFICATION.md OWN-C |
| 12 | Owner-cert OWN-E (raw_materials) | **Raw-material index tile lumps DAMAGED rolls into "Used"** (`RawMaterialDashboardView.get_context_data`, dashboard.py: `used = total - available` — with 32 total / 2 not_used / 29 used / 1 damaged the tile renders "30 Used"; browser-proven as owner 2026-07-13). The canonical cloth dashboard one click deeper splits Damaged as its own tile (V1.1 item-1 posture intact — verified same session). Display-honesty only: no permission gate, no money math, no persisted data reads this number. Fix when touched = subtract damaged from the tile (`used = total - available - damaged`) + optional Damaged count on the tile row. | Display-only aggregation label on a summary tile; truth is correct on the canonical dashboard; out of OWN-E fix mandate (0 confirmed bugs; fix-only-confirmed rule); evidence: OWNER_VISIBILITY_CERTIFICATION.md OWN-E |
| 13 | Office-cert OFF-A (production) | **Dead `can_edit_financials` context flag on the layering workspace** ([stage_views.py:329](../config/production/views/stage_views.py#L329) computes `can_edit_financials = user_can_edit_financials(user)`, passed into the template context at [:404](../config/production/views/stage_views.py#L404)) — NO template consumes the key (grep across `config/**/*.html` = zero references; contract §2.2.1 pre-registered observation (a), judged in OFF-A). No security/functional impact: the financial service guard (`update_roll_details`/`bulk_create_rolls`) is the real gate and is independent of this flag; the layering page is ProductionRole-gated so the accountant never renders it; even if rendered the flag drives nothing. Fix when touched = drop the two dead lines (or wire a template consumer if a financial control was intended there). | Dead read-only context variable, no consumer, no gate depends on it; out of OFF-A fix mandate (0 confirmed bugs, engine-frozen U1, smallest-change); evidence: OFFICE_SUPPORT_ROLE_CERTIFICATION.md OFF-A |
| 14 | Office-cert OFF-B (storefront/accounts) | **`STOREFRONT_ROLES` is a dead lever — zero functional consumers** (grep census 2026-07-13: defined at [permission_service.py:31](../config/accounts/services/permission_service.py#L31) and re-exported by the two services `__init__` files, consumed NOWHERE). BOTH live storefront gates hand-build the identical `{ROLE_SUPER_ADMIN, ROLE_LISTING_TEAM}` pair instead: `ListingTeamMixin` ([listing_views.py:26-27](../config/storefront/views/listing_views.py#L26-L27), contract §2.2.2 pre-registered observation, judged in OFF-B) AND the sidebar Storefront section (`permission_service.py:316-323` `_any_role(...)`). Membership equivalence proven live both directions at OFF-B (listing+SA admitted; manager/worker/accountant refused). Risk = maintenance trap: editing the constant changes nothing (silently), and a future divergence between the two hand-built copies would split menu-visibility from page-access. Fix when touched = make both gates consume `STOREFRONT_ROLES`. | Zero functional impact today (equivalence certified); engine-frozen U1 + role-set-adjacent edit not licensed by any confirmed bug (contract §10 posture); out of OFF-B fix mandate (0 confirmed bugs); evidence: OFFICE_SUPPORT_ROLE_CERTIFICATION.md OFF-B |
