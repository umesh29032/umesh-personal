# QA Audit — 2026-06-02 (stage-costing + payroll/settlement + expense app)

Verify-first end-to-end QA of the uncommitted change stack on `new_flask_app`
(stage costing, worker payroll rebuilt to **settlement-based**, `expense` app,
unified tracking, dashboards). Two-track method: (1) adversarially-verified
static code audit, (2) live browser testing on `:8000` as Super Admin + Cutting
Master (karigar) + DB cross-checks.

> ⚠️ The dev DB (`inventory_db`) was being mutated by the owner **during** this
> audit (the demo settlement SETL-0001 disappeared mid-run). Live-data values
> below are point-in-time snapshots, not stable truth.

## Baseline
- `manage.py check` — clean (0 issues).
- `makemigrations --check` — no drift.
- Test suite — **270 passing** (`config.settings.local`, run from `config/`;
  note: discovery must start in `config/`, not repo root, or it finds 0 tests).

## Feature map (new/changed surface tested)
| Area | Route(s) | Role gate | Result |
|---|---|---|---|
| Manufacturing Costing | `/production/costing/` | management | 200, ₹1050 verified |
| Payroll Overview | `/expense/payroll/` | management | 200 |
| My Earnings | `/expense/my/` | any (self-scoped) | 200 |
| Worker Detail | `/expense/workers/<pk>/` | self or management | 200/403 scoped |
| Settlement | `/expense/workers/<pk>/settle/` | management | 200 |
| Record Advance | `/expense/advances/add/` | management | 200 |
| Worker Profile | `/expense/workers/<pk>/profile/` | management | 200 |
| Flow cost editor | `/production/products/<pk>/flow/` | perm-gated | 200 |
| Cutting allocation | `/production/addas/<code>/cutting/workspace/` | cutting skill | covered by tests |

## Browser results
- **All new pages: HTTP 200, zero console errors, zero failed network** (only
  Google Fonts, 200). Verified as Super Admin.
- **Sidebar gating correct**: karigar sees `Dashboard` + `My Earnings` only;
  NO Manufacturing Costing / Payroll / Record Advance / admin items. The
  sidebar whitelist-fallback fix holds.
- **Mobile (375×812): no horizontal overflow** on costing, payroll, products,
  my-earnings, worker-detail, settlement-form. Costing "N unpriced" note now
  stacks under the cost (fix #7).

## Security matrix (karigar wa-audit, direct URL)
| URL | Expected | Got |
|---|---|---|
| `/expense/my/` | 200 (own) | ✅ 200 |
| `/expense/workers/17/` (self) | 200 | ✅ 200 |
| `/expense/payroll/` | deny | ✅ 403 |
| `/expense/advances/add/` | deny | ✅ 403 |
| `/production/costing/` | deny | ✅ 403 |
| `/expense/workers/18/` (other worker) | deny | ✅ 403 |
| `/expense/workers/1/` (super admin) | deny | ✅ 403 |
| `/expense/workers/18/settle/` | deny | ✅ 403 |
| `/production/stages/` | deny | ✅ 403 |

**No cross-worker financial leak. No management-page leak. AJAX/POST gated.**
Note: `utest@gmail.com` is roled **Manager** (in `MANAGEMENT_ROLES`), NOT a
karigar — its costing/payroll access is correct. The "cutting master" label in
the creds note is stale. True karigar test used `wa-audit@test` (pk17).

## Payroll math (verified vs DB via real services, point-in-time)
Worker A (wa-audit): earned 10×₹5=₹50 ✅; settlement SETL-0001 paid ₹20 +
recovered ₹30 → `amount_paid + advance_deducted = payable_before` (20+30=50) ✅;
pending_payable 0 ✅; advance outstanding 70 ✅. Worker B (wb-audit): 15×₹5=₹75,
pending 75 ✅. Costing worker-earnings ₹225 = utest 100 + wb 75 + wa 50 ✅.
**All internally consistent at snapshot time.**

## Live functional verification (writes, then cleaned up)
Driven through the authenticated browser session (real auth + CSRF + views +
services + DB), on throwaway targets, all reverted after:
- **Record Advance form** → ₹40 for wb-audit persisted; did NOT debit payable
  (loan-pool semantic ✓).
- **Create Settlement form** → SETL-0001 (₹45 cash + ₹30 recovery); invariant
  `paid+recovered=payable_before` (45+30=75) ✓; payable→0, advance→10, settled
  →45; 2 linked ledger debits ✓.
- **Allocation** (`cutting-item-allocate`) → 5×₹4=₹20 booked as
  StageWorkAssignment + ledger credit ✓.
- **#3 fix LIVE** → POST-delete an allocated item returned **200** (302→
  workspace), NOT a 500 ProtectedError; item survived; view catches the
  ValidationError. Confirms the fix in the real request path.
- **Void** (`cutting-allocation-delete`) → assignment voided + credit reversed ✓.
- Teardown: throwaway Adda NIKKAR-002 + all rows deleted, NIKKAR cutting cost
  restored to unpriced, wb-audit back to baseline, `check` clean, 0 residue.

NOT driven live (covered by 270 tests + seeded 3-PATTI-003 lifecycle): the
layering/pattern stage widgets, breakup→bundle fancy-select UI, and the
complete→barcode→reopen sequence. `reopen_cutting` is safe by inspection (it
does not delete bundles; the only PROTECT-crash path was delete_bundle/item,
now guarded + proven live).

**Note (latent UX fragility, not a bug):** the `worker` field on the advance +
allocation forms is a fancy-select over a `required` native `<select>`. Setting
the native select programmatically doesn't stick; a real click on the visible
option does (verified). If the widget ever fails to sync, the browser blocks
submit with "invalid form control … not focusable" and no visible error.

See [bugs_found.md](bugs_found.md) and [fixes_applied.md](fixes_applied.md).
</content>
</invoke>
