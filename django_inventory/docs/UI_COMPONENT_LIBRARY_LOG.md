---
id: ui-component-library-log
type: evidence-log
status: active
owner: append-only
scope: all — Campaign Phase 10 (UI Component Library) evidence
anchors: docs/campaign_contracts/PHASE_10_UI_COMPONENT_LIBRARY.md, UI_COMPONENTS.md, config/accounts/templates/accounts/base.html
verified: 2026-07-16
---

# UI COMPONENT LIBRARY LOG — Campaign Phase 10 (created UIL-0, append-only)

> Contract = procedure ([PHASE_10_UI_COMPONENT_LIBRARY.md](campaign_contracts/PHASE_10_UI_COMPONENT_LIBRARY.md));
> this log = what was censused, certified, consolidated, and proven. The lasting canon =
> the certified library document (UI-D1); this log is its proof.

---

## UIL-0 — Charter + gate + ratification — EXECUTED 2026-07-16 · ⏸ AWAITING OWNER UI-D1..UI-D9

**Owner order honored:** "Begin Phase 10 (UIL-0). Follow the contract exactly. STOP at the
next owner gate." The order carries no ratification language for UI-D1..UI-D9 → per the FIX-0
precedent no owner answer is fabricated; the Decision half stays pending; **the next owner
gate = this decision pack.** Zero census work, zero certification, zero template/CSS edits,
zero code; battery never runs at UIL-0 (entry baseline **1530/1530** stands).

### 1. Gate (contract §7 UIL-0 row) — PASS

| Check | Result |
|---|---|
| Phase 9 closed | ✓ — [DOCUMENTATION_GENERATION_LOG.md](DOCUMENTATION_GENERATION_LOG.md) §GEN-F "🏁 PHASE 9 VERDICT: GENERATION-COMPLETE-WITH-DOCUMENTED-DEFERRALS · PHASE 9 CLOSED"; owner acceptance verbatim 2026-07-16 ("Phase 9 is accepted.") |
| Master-index order | ✓ — Phase 10 follows Phase 9 in the LOCKED order; Standard `frozen-v1` + Phase-9 pipeline live |
| Evidence log absent pre-session | ✓ — this file created now |
| git state | HEAD `49404001` · 0 staged · 2 stashes ✓ |
| Battery baseline (entry) | **1530/1530** (status dashboard; battery-bearing = UIL-D waves only) |

### 2. Census sources verified present (§7 UIL-0 row)

| Source | State |
|---|---|
| `UI_COMPONENTS.md` (repo root — the canon, CLAUDE.md rule 9) | PRESENT, 441 lines |
| `docs/DESIGN_SYSTEM_SPEC.md` + `docs/UI_COMPONENTS_CATALOG.md` (frozen reference pair) | PRESENT both |
| `docs/FRONTEND_AUDIT_2026_07_05.md` (FE-1..FE-5 + 7 permanent UI rules) | PRESENT, 169 lines |
| `docs/HTML_AUDIT_MASTER.md` + `docs/HTML_CANONICAL_CANDIDATES.md` (paused stream — census INPUTS only, UI-D9) | PRESENT, 248 + 953 lines |
| `config/accounts/templates/accounts/base.html` (single-CSS-source) | PRESENT |
| Template census denominator (preview; UIL-A recounts mechanically) | 169 `.html` files under `config/**/templates/` |

### 3. Standing owner context recorded at charter (2026-07-16, same day)

- **Owner permanent guidance (verbatim rulings, post-Phase-9 KOS architectural review):**
  roadmap 0–22 unchanged · KOS Target Vision = long-term architecture (distinction permanent)
  · **KOS Consolidation Program = owner-approved future planning item, deferred until after
  Phase 22, not yet written anywhere** · every remaining phase records future-KOS
  compatibility in its Design Record where natural, WITHOUT scope expansion · no new parallel
  documentation systems · strengthen the single-canonical model.
- **Phase-10 KOS-compat note (record-only, no scope change):** UI-D8 already reserves exactly
  the right lane — component nodes/edges = KG-D6 minor-extension PROPOSAL + library entries =
  GEN-D9 target candidate, both written into the UIL-F handoff, neither built here. The
  2026-07-16 review additionally flags for the future consolidation program (NOT this phase):
  post-UIL-F role reconciliation makes DESIGN_SYSTEM_SPEC/CATALOG archive candidates once
  UI_COMPONENTS.md is the certified library (their disposition = a later owner decision).
- Post-Phase-9 snapshot refresh (Phase-0 D4 cadence) is DUE — owner action, not a UIL gate.

### 4. ⏸ Decision pack — UI-D1..UI-D9 (defaults binding unless overridden; contract Appendix A)

| # | Item | Default (verbatim from the frozen contract) |
|---|---|---|
| UI-D1 | Library artifact form | `UI_COMPONENTS.md` upgraded IN PLACE to the Standard-compliant certified library (already the canon — no parallel doc, no fork); DESIGN_SYSTEM_SPEC + UI_COMPONENTS_CATALOG remain the frozen reference pair, cross-linked with roles stated; no Storybook/new tooling |
| UI-D2 | Taxonomy | §6.2 families as census classes (layout · navigation · forms🔒 · inputs/select/date🔒 · tables · cards · badges/chips · dialogs · notifications · filters · pagination · search · dashboards · domain widgets [production/financial🔒/stage/inventory] · export/print · auth🔒); new families only via dated amendment |
| UI-D3 | New-component policy | ZERO new shared components in Phase 10; exception = design-frozen audit + rule-of-3 evidence + owner approval, recorded per component |
| UI-D4 | Consolidation scope | Owner selects the execution queue at UIL-C from the duplication register; rule-of-3 binding; frozen-family clusters excluded unless per-cluster gated |
| UI-D5 | Accessibility baseline | Pragmatic checklist (labelled inputs · touch-target floor · state-color contrast spot-checks · observed keyboard behavior) — NO WCAG conformance claim (no repo standard exists) |
| UI-D6 | Visual-regression method | Before/after screenshot pairs at 360/768/1280 per touched page, dev-server restarted, archived with the log; no new tooling |
| UI-D7 | Token registry | Extracted from base.html as the CLOSED set (spacing/typography/colors+state/icons/borders/radius/elevation/breakpoints/animation); CSS breakpoints vs the 3 verification widths enumerated separately; new token = owner-gated |
| UI-D8 | Graph/generation interface | Component nodes/edges = KG-D6 minor-extension PROPOSAL + library entries = GEN-D9 target candidate — both into the UIL-F handoff, neither built in Phase 10 |
| UI-D9 | Paused HTML-audit stream | HTML_AUDIT_MASTER + HTML_CANONICAL_CANDIDATES consumed as census INPUTS; the stream stays paused (not resurrected, not closed); disposition = later owner decision |

**Recommendation: accept all nine defaults.** They encode the standing owner rules already in
force (design-frozen · rule-of-3 · tokens-only · mobile-first · no new tooling) and UI-D8 is
already the KOS-vision-compatible lane. "I accept the defaults" suffices; overrides recorded
verbatim per item.

### 5. Scope discipline

Files touched this sub-phase: this log (NEW) · DOCUMENTATION_INDEX (log row, §11c) · status
file · memory. Zero templates/CSS/code; zero census/certification work (UIL-A/B's job);
battery not run. git HEAD `49404001` · 0 staged · 2 stashes.

**Next: owner answers UI-D1..UI-D9 → UIL-0 CLOSED → UIL-A component + token census
(owner-gated).**

### 6. UIL-0 CLOSURE — owner rulings 2026-07-16 (verbatim)

> "I accept all default decisions. UI-D1 — ACCEPT … UI-D9 — ACCEPT. No overrides. Continue
> following the frozen Phase 10 contract exactly. Do not expand scope. Do not implement any
> future KOS ideas yet. Continue recording future KOS compatibility only where the Design
> Record naturally allows it. The roadmap remains unchanged. Proceed to UIL-A."

All nine defaults RATIFIED → contract Appendix A Decision half filled (designated fill).
**UIL-0 CLOSED 2026-07-16.** UIL-A authorized by the same order (same-session continuation =
owner-ordered).

## UIL-A — Component + token census — DONE 2026-07-16

**Method:** mechanical main-thread instruments (authoritative: file inventory, pattern greps,
token extraction, hotspot ranking) + a 10-agent parallel read-only census sweep (supplemental
per §5/U7; 10/10 returned, 0 failed, per-group file counts sum to the mechanical denominator
exactly) + **main-thread mechanical verification of every headline duplicate claim (7/7
CONFIRMED)** before registration. Read-only phase: zero template/CSS/code edits; battery not
run (1530/1530 stands). Raw data: scratchpad `uil_a/` (occurrences.json · templates.json ·
census_agents.json).

### 1. Template census (denominator = counts reconcile)

**169 templates** = accounts 21 · expense 11 · machines 3 · patterns_ai 37 · production 50 ·
raw_materials 11 · storefront 7 · tracking 7 · project-level `config/templates/` 22
(13 inventory + 5 shared + 3 error pages + public_home). 31 underscore partials; 5 `shared/`
partials (`_auth_shell` · `_datatables_vendor_css/js` · `_export_buttons` · `_form_styles`).
126 extend a base; 43 standalone/partial (iframe shells, print sheets, auth shell family,
error pages, widget templates). Sweep coverage: 169/169 classified.

Key occurrence counts (files containing; full per-app matrix in scratchpad): panel 53 ·
page-header 50 · card 49 · table 43 (table-responsive 29 · data-label 36 · DataTables init 9)
· sticky family 38 · form-A `.field`/form-shell 58 · form-B `.sf-*` 9 · select 34 ·
hero-strip 18 · kpi 18 · badge 28 · delete-confirm pages 13 · btn family 113 ·
`{% block extra_head %}` 100.

### 2. Design-token registry (UI-D7 — extracted, CLOSED set)

**127 unique custom properties** defined in `base.html` (48 dual-defined light+dark):
colors/brand (ink/copper/cream/stone/gray-scale/primary-blue) · status ×10 · semantic
(text/surface/border/card) · typography (fs ×9 · fw ×4 · lh ×2 · font-display/sans) ·
spacing `--space-1..16` ×9 · radius ×9 · shadows ×4 · inputs ×8 · select-arrow ×4 · sidebar
×7 · nav ×3 · topbar ×2 · z-scale ×6 · `--control-h`/`--touch-min` · misc (filter/form).
**CSS breakpoints (media queries): 360 · 480 · 560 · 600 · 768 · 1024 px + reduced-motion**
— distinct from the 3 VERIFICATION widths 360/768/1280 (enumerated separately per UI-D7).
Motion: 1 keyframe (`slideDown`) · 34 transition rules. Registry basis for the UIL-F library
doc. Secondary token sets exist by design in standalone owners: `_auth_shell.html`
(`--auth-*` knobs, own palette) · `public_home.html` (own `:root`) · chromeless iframe shells
(local token subsets) — recorded, not defects (no base.html in those documents).

### 3. Component taxonomy census (§6.2) — all 17 families occupied

Every family has occurrences; per-template classification = census_agents.json (169 rows).
Owners already certified by prior rules and confirmed live: `_form_styles` (form-shell) ·
`_user_form_styles` (accounts form skin) · `_fancy_controls` (675-line FancySelect/Date
single owner) · `_auth_shell` (auth) · `_export_buttons` (export-print) ·
`_time_log_styles` (history accordion CSS) · base.html (layout/tables/cards/badges/msg/
DataTables helper). **Taxonomy-gap check (§7 stop test): NOT triggered as a stop** — census
completed — but the unclassifiable residue is RECURRING, not noise (~50 rows folding into ~5
patterns) → classified owner item OD-UIL-1 below (dated UI-D2 amendment lane exists for
exactly this).

### 4. Duplication register (synthesized cross-group; ✔ = main-thread mechanically verified)

| # | Cluster | Files | Frozen? | Evidence |
|---|---|---|---|---|
| D-UI-01 ✔ | **Navy→copper hero gradient re-declared in page CSS** (`linear-gradient(135deg,#0a1628…#b87333 130%)`) | **23** corpus-wide (grep-verified list in scratchpad; canon lives in `_form_styles`/base.html) | no | biggest token/canon-shadowing cluster in the repo |
| D-UI-02 ✔ | Error-page skeleton | 3 (403/404/500) | no | `<style>` blocks **md5-identical** (`9811d631…` ×3) |
| D-UI-03 ✔ | DataTables list recipe incl. **byte-identical `<th>` inline-style string ×14** | 3 accounts lists (+2 storefront clones = same recipe class) | no | grep 6+5+3 = 14 exact |
| D-UI-04 ✔ | 6-digit OTP widget (markup + paste/nav/auto-submit JS + countdown) | 3 (otp · reset_otp · signup_otp) | **AUTH 🔒** | maxlength-row grep ×3 |
| D-UI-05 | Delete-confirm pages: 3 divergent hand-rolled recipes in accounts + 2 storefront clones, while `role_confirm_delete` composes the base.html `.confirm-card` canon | 6+ | no (page class) | agent-read; canon EXISTS, drift class |
| D-UI-06 ✔ | Sticky action bar re-implemented in page CSS (`position:sticky` + `backdrop-filter`) | 5 expense + sidebar_access_list (+3 more variants) | financial-ADJACENT (expense money pages) | backdrop-filter grep ×5 in expense |
| D-UI-07 ✔ | **Status/verdict color trio hard-coded** (`#20603c`/`#a26a00`/`#b91c1c` + chip bgs) while `--status-*` tokens EXIST | **18** corpus-wide | no | grep ×18; token-shadowing (backlog-#3 class, permanent-rule-3 violation census) |
| D-UI-08 ✔ | Amber note/law box (`#fdf7e7` + `#eadfc0`) | 7 (patterns_ai) | no | grep ×7 |
| D-UI-09 | `.filter-card` CSS block copies | 5 (rm ×2 · production ×2 · tracking) | no | agent-read, vocabulary-documented pattern |
| D-UI-10 | Cream-input/form-shell RE-IMPLEMENTATIONS (machines hex copies ×2 · expense form-CSS ×3 · `roll_bulk_form` full re-impl · patterns_ai local mini-system + `label.f` substitutes ×3) | ~10 | **FORMS 🔒** | agent-read; frozen-family drift register |
| D-UI-11 | data-label mobile stacking re-implemented per page + **PA-15 dead-label candidates**: `adda_settlement_list` · `payroll_overview` · `costing` · `access_control` (✔ verified: `@media` present, NO `display:none` stacking rule); stage-panel partials = host-dependent (PA-15-3 class); `user_create` + `_stage_panel_generic` have own scoped stacking (live) | ~10 | expense = financial 🔒 | main-thread grep per file |
| D-UI-12 | Time-log accordion markup twins (`_adda/_roll_events_accordion`) — CSS already deduped to `_time_log_styles` | 2 | no | agent-read |
| D-UI-13 | Misc small: toolbar spacer span ×8 (patterns_ai) · back-link footer ×3 (machines/tracking) · swatch dots ×3 (rm) · uppercase micro-label inline ×4+ · cp-summary block ×3 · standalone stage-workspace wrapper ×3 · Layout-Summary table CSS ×3 · print-tile system ×2 · panel-family scoped copy ×2 · iframe shells ×2 | various | no | agent-read, itemized in census_agents.json |
| D-UI-14 | Pre-shell standalone auth pages duplicating the auth palette/monogram/OTP (`signup.html` · `signup_otp.html` — S2-closed/dormant) | 2 | **AUTH 🔒** | agent-read |

### 5. Rule-of-3 candidates (≥3 genuinely-identical occurrences, NON-frozen — the UIL-B/C feed)

D-UI-01 (23) · D-UI-07 (18) · D-UI-08 (7) · D-UI-06 (5+) · D-UI-09 (5) · D-UI-03 (3–5) ·
D-UI-02 (3) · back-link footer (3) · cp-summary (3) · stage-workspace wrapper (3) ·
Layout-Summary table (3) · expense cream-form CSS (3 — but FORMS-frozen flag) · swatch dots
(3). Two-occurrence clusters (registered, NOT extractable per rule-of-3): D-UI-12 · print-tile
×2 · panel-family copy ×2 · iframe shells ×2 · filter-card pair-subsets.
**NOTE (normative for UIL-C):** rule-of-3 licenses extraction; several clusters need NO new
component — the canon already exists (hero-strip, `.confirm-card`, `--status-*` tokens,
sticky-actions, filter-card) and the fix class is "re-point page to canon" (INERT), which is
consolidation, not component-building (UI-D3 untouched).

### 6. Frozen-family occurrence register (read-only; any touch = per-cluster owner gate)

| Family 🔒 | Owner (live) | Census | Drift inside the family (register-only) |
|---|---|---|---|
| Forms (.field/.sf-*) | `shared/_form_styles` + `accounts/_user_form_styles` + base.html `.sf-*` | 58 files form-A · 9 form-B | D-UI-10 re-implementations (~10 files) |
| Inputs/Select/Date | `accounts/_fancy_controls` (675-line single owner) | 34 select files · 5 date · 6 `data-no-fancy` (4 = the owner partial/base + print contexts) | healthy — no competing implementation found |
| Financial (money-family) | base.html money classes + hero-strip.copper | 11 expense templates · hero-copper ×5 · `{% money %}` usage | sticky/table re-impls on money pages (D-UI-06/11) |
| Auth | `shared/_auth_shell` (--auth-* knob system) | 7 shell-composed pages | D-UI-04 OTP triplication · D-UI-14 pre-shell standalone pair |

### 7. Hard-coded-value findings (rule-3 census; fix-when-touched class)

Corpus (excl. the 6 CSS-owner partials + base.html): **1,027 `style="` occurrences in 120
files · 897 six-digit-hex occurrences in 99 files.** Top hotspots: `_stage_panel_cutting`
(79/10) · `_stage_panel_layering` (39/37) · `cutting_table` (21/47) · `adda_detail` (33/30) ·
`_stage_panel_cutting_pattern` (48/6) · `studio_work` (34/14) · `role_form` (23) ·
`user_dashboard` (22) · `user_list` (35 incl. the D-UI-03 ×14 string). Classified exemptions:
`styleguide.html` (41 = token-demo swatches, deliberate) · `_worker_report_styles`/
`_auth_shell`/`public_home` (standalone CSS owners — definitional hex). Bare-table census:
14 files with `<table>` and no `.table-responsive` (4 = PA-15-1 text-only-safe candidates,
rest = D-UI-11 register; expense/money files carry the financial-frozen flag).

### 8. Graph/KOS-compatibility observations (UI-D8 — record only, nothing built)

The census IS the future component-node source: per-family owner tables → `component` node
kind candidates (KG-D6 additive-minor, already registered) · 169 template paths → template
nodes (additive-minor register row, now with measured denominators: 169 templates · 17+~5
families · 127 tokens) · occurrence tables → `uses_component` edge evidence (source-taggable:
"census") · token registry → component attributes. Handoff lands at UIL-F per UI-D8.

### 9. Classified owner items (⏸ decisions for UIL-B entry — none blocks census closure)

| # | Item | Default/recommendation |
|---|---|---|
| OD-UIL-1 | **Taxonomy gap (recurring unclassifiable, ~50 rows → ~5 patterns):** icon-dispatch partials (`_nav_icon`/`_section_icon`) · behavior-only JS partials (`_autosave`/`_stage_panel_collapse`) · iframe embed-shells + postMessage protocol (stage panels/user_dashboard) · print-layout pages (barcode sheet, true-scale A4 tile system) · interactive SVG workspaces (patterns_ai editors) — plus matrix/stepper/marketing one-offs | dated **UI-D2 amendment** adding ~5 census families (recommend ACCEPT at UIL-B start so canon certification covers them; volume is real, silence would bin them) |
| OD-UIL-2 | `styleguide.html` inline-style exemption (token-demo swatches) | record-exempt (recommended) |
| OD-UIL-3 | Dormant pre-shell auth pages (signup/signup_otp — S2-closed) carried in the AUTH frozen register | no action in Phase 10 beyond the register; disposition = owner at UIL-C if ever queued (frozen gate applies) |

### 10. Scope discipline

Files touched: this log · status file · memory (+ contract Appendix A at the UIL-0 closure
earlier this session). Zero template/CSS/Python edits; battery not run (read-only sub-phase);
sweep agents read-only (0 failures — the supplemental-only rule satisfied: every registered
headline claim re-verified main-thread). git HEAD `49404001` · 0 staged · 2 stashes.

**UIL-B READY:** census complete with arithmetic (169/169; counts reconcile) · duplication
register + rule-of-3 candidates on the table · frozen map drawn · OD-UIL-1..3 await owner at
UIL-B entry.

_Section closed 2026-07-16. Next: UIL-B canon certification (owner-gated)._

### 11. UIL-A CLOSURE — owner rulings 2026-07-16 (verbatim)

> "UIL-A is accepted. OD-UIL-2 — ACCEPT. OD-UIL-3 — ACCEPT. For OD-UIL-1, do not immediately
> expand the top-level taxonomy. Before introducing new families, first perform a
> classification review: determine whether each recurring pattern can be represented as a
> subtype of an existing UI family. Only if a pattern genuinely cannot fit into the current
> taxonomy should a new top-level family be proposed. Keep the taxonomy as small and stable
> as possible. Simplicity and long-term maintainability take priority over classification
> granularity. If, after that review, some patterns still require new top-level families,
> present the minimal amendment with clear justification. … After completing that
> classification review, continue with UIL-B."

OD-UIL-2 CLOSED (styleguide token-demo exemption recorded) · OD-UIL-3 CLOSED (dormant auth
pair register-only; frozen gate applies if ever queued) · OD-UIL-1 → classification review
below (UIL-B §1). Same-session UIL-B continuation = owner-ordered.

## UIL-B — Canon certification — DONE 2026-07-16

Documentation-only sub-phase (contract §7: no edits, battery never — 1530/1530 stands).

### 1. OD-UIL-1 classification review (owner-ordered subtype-first discipline)

Every recurring unclassifiable pattern tested against the existing 17 families:

| Census pattern | Disposition | Existing family (subtype) |
|---|---|---|
| Icon-dispatch partials (`_nav_icon`/`_section_icon`) | SUBTYPE — sidebar iconography (they exist only as sidebar feed) | navigation |
| Behavior-only JS partials (`_autosave` · `_stage_panel_collapse`) | SUBTYPE — behavior partial OF its owning family | forms (autosave) · domain:production (collapse) |
| Iframe embed-shells + postMessage protocol (`stage_panel_embedded` · `worker_report_embedded` · `stage_advanced_bounce` · host iframes) | SUBTYPE — embedded-shell; every instance is a production stage surface | domain-widgets:production |
| Print-layout documents (`barcode_print_sheet` · true-scale A4 tile system ×2) | SUBTYPE — print-document (the family already holds print CONTROLS; documents = its second grain) | export-print |
| Interactive SVG workspaces + readiness matrix + verdict cards (patterns_ai editors) | SUBTYPE — but the domain-widgets family's enumerated domain list (production/financial/stage/inventory) lacks the patterns domain → **the ONE genuine gap** | domain-widgets:**patterns** (subtype ADDITION — amendment A1) |
| Stepper/workflow rail (patterns dashboard) · placeholder tiles · pivot matrix | SUBTYPES | dashboards |
| Standalone documents (public_home marketing · error-page trio) | SUBTYPE — standalone document | layout |
| OTP widget · step-progress · auth-vars knobs | SUBTYPES (already censused under auth 🔒) | auth |
| Toggle switches · pwd-meter · avatar preview · chip-picker · cropper widget · Django widget-override templates · live JS preview | SUBTYPES — input/feedback widgets | forms 🔒 |
| Correction-chain breadcrumb | SUBTYPE | navigation |
| Month prev/next navigator | SUBTYPE — temporal pager | pagination |
| Sort-order chips · active-filter chip strip | SUBTYPES | filters |
| `<details>/<summary>` disclosure (machine summary-cards · void-form-in-cell · accordions) | SUBTYPE — disclosure | dialogs-confirmations |
| Precondition-blocker boxes (`worker_fnf`) · amber note boxes | SUBTYPES | notifications |
| Vendor version-pin includes (`_datatables_vendor_*`) | SUBTYPE — vendor infrastructure | tables |

**Verdict: ZERO new top-level families required.** The taxonomy stays at 17. Minimal
amendment: **A1 (dated, Appendix A)** — domain-widgets subtype list gains **patterns**
(justification: patterns_ai is a real product domain with 37 templates and its own widget
vocabulary; the other four domains are enumerated, its absence was an authoring-time gap,
not a new classification concept). Subtype labels above are census vocabulary, not new
family rows — recorded here as the classification convention.

### 2. Canon certification table — one owner per family, reference page named

🔒 = frozen family (owner implementations read-only; drift = registered, never touched
without per-cluster gate). Refs = template paths; occurrence lists = census_agents.json.

| Family | THE certified owner pattern | Reference page | Notes / registered drift |
|---|---|---|---|
| layout | base.html `.shell/.sidebar/.content/.topbar`; hero = `.hero-strip` (base.html) + form-shell hero (`shared/_form_styles`); panels = `.panel`/panel-num; sticky = `.sticky-bar`/`.sticky-actions` | `production/stage_form.html` (THE form-shell reference, permanent rule 1) | D-UI-01 (23-file gradient re-declare) + D-UI-06 (sticky re-impl) consolidate TO this canon; standalone-document subtype: `_auth_shell`/`public_home`/error pages own their shells by design |
| navigation | base.html sidebar + `_nav_icon`/`_section_icon` dispatch; `.tabs`; breadcrumbs | base.html (any page) | correction-chain = expense-local subtype, documented variant |
| forms 🔒 | **Dual-canon BY LAW (documented "pick one per page"):** Style A `.field` — owners `shared/_form_styles.html` + `accounts/_user_form_styles.html`; Style B `.sf-*` — owner base.html | A: `production/stage_form.html`; B: `storefront/listing/product_form.html` | NOT a conflict — certified dual with domain split (app pages = A, storefront = B). Drift registered: D-UI-10 re-implementations ~10 files incl. `role_form` `.form-card` variant + `roll_bulk_form` re-impl |
| inputs-select-date 🔒 | `accounts/_fancy_controls.html` (single 675-line owner; opt-out only via `data-no-fancy`) | `inventory/styleguide.html` demo + any form | healthy — zero competing implementations (census) |
| tables | `.table-responsive` + `td[data-label]` (base.html stacking) + `initFancyDataTable` + `shared/_datatables_vendor_*` version pins; `.tbl` = documented secondary | `accounts/user_list.html` (DataTables list recipe) | reference page itself carries the D-UI-03 inline-`<th>` drift — canon = the vocabulary pattern, page listed as best-live-example-with-registered-drift; D-UI-11 re-impls + PA-15 candidates registered |
| cards | base.html `.card/.card-clip/.card-header/.kpi/.stat-grid/.stat-card` | `raw_materials/cloth_dashboard.html` | — |
| badges-chips | base.html `.badge*` + `--status-*` tokens; `.skill-tag`; `.chip-pick` (`_user_form_styles`) | `accounts/user_list.html` | D-UI-07 (18-file hex trio) consolidates to `--status-*` tokens |
| dialogs-confirmations | base.html `.confirm-card` (delete-confirm canon) + `confirm()` law for money/destructive; disclosure subtype = native `<details>/<summary>` | `inventory/role_confirm_delete.html` | D-UI-05: 3 divergent accounts recipes + 2 storefront clones = drift to this canon (canon pre-exists; no arbitration needed) |
| notifications | base.html `.msg/.msg-stack` (global render) | base.html | patterns_ai local `{% if messages %}` loops ×2 = drift; blocker/note boxes = subtypes |
| filters | `.dt-filter-row` (inside-card, preferred) + `.filter-card` page pattern WITH mandatory ≤600px stack rule + `.skill-filter-chip` | `accounts/user_list.html` (dt-filter-row) · `raw_materials/roll_list.html` (filter-card) | **`.ke-toolbar` = DEPRECATION NOMINEE** (vocabulary already says avoid; 3 occurrences) — owner decision lands with the UIL-C queue |
| pagination | DataTables `.dt-bottom` (global CSS); month-navigator = temporal subtype | any DataTables list | — |
| search | DataTables `.dt-search-row` | same | — |
| dashboards | base.html `.kpis/.kpi` + `.stat-grid/.stat-card`; tiles/rails/matrices = subtypes | `raw_materials/cloth_dashboard.html` | accounts `home.html` legacy demo dashboard = drift (hardcoded KPIs, inline JS) |
| domain:production | stage-panel partial family (`_stage_panel_*` dispatch) + embed-shell protocol + `_workers_widget` + behavior partials | `production/adda_detail.html` | embedded-shell subtype certified here (OD-UIL-1) |
| domain:financial 🔒 | money-family classes (`.hero-strip.copper` · `.stat-card.payable` · money display) per the A-scope vocabulary | `expense/adda_settlement_detail.html` (settlement family) | sticky/table re-impls on money pages = registered, gate-required |
| domain:stage | worker-report body + `_worker_report_styles` (stage trio/checklist) | `production/worker_report_embedded.html` | — |
| domain:inventory | `_time_log_styles` (accordion CSS owner) + `_roll/_adda_events_accordion` pair; roll/stock cards | `raw_materials/roll_detail.html` | D-UI-12 markup twins registered (2 occurrences — below rule-of-3) |
| domain:patterns (A1) | `_design_row.html` atom + SVG workspace vocabulary + `_readiness_matrix` | `patterns_ai/cutting_table.html` | local mini design-system re-impls = registered drift (D-UI-10 class) |
| export-print | `shared/_export_buttons.html` (controls) + print-document subtype (`barcode_print_sheet` A4 system) | tracking dashboard (buttons) · `tracking/barcode_print_sheet.html` (documents) | true-scale tile pair (×2) = below rule-of-3, registered |
| auth 🔒 | `shared/_auth_shell.html` (--auth-* knob system) | `accounts/login.html` | D-UI-04 OTP triplication + D-UI-14 dormant pre-shell pair = registered, gate-required |

**Conflict resolution required from owner: NONE.** Every family resolved to an existing
single owner (or the documented dual-form law). Competing implementations everywhere
classified as DRIFT toward an existing canon — consolidation queue material (UIL-C), not
canon arbitration. One deprecation nominee flagged (`.ke-toolbar`).

### 3. Duplicate clusters → queue feed (rule-of-3 + frozen flags, final)

Queue-eligible (non-frozen, ≥3, canon exists — INERT re-point class): D-UI-01 hero-gradient
(23) · D-UI-07 status-hex→tokens (18) · D-UI-08 amber box (7) · D-UI-06 sticky (5+; expense
pages carry financial-ADJACENT flag — CSS-only swap, money markup untouched, still
per-cluster caution) · D-UI-09 filter-card (5) · D-UI-03 DataTables th-strings (3 lists) ·
D-UI-02 error-page trio (3) · D-UI-05 delete-confirm re-points (5) · misc ×3s (back-link,
cp-summary, workspace wrapper, Layout-Summary table, swatch dots). Frozen-gated (owner
per-cluster gate mandatory): D-UI-04 OTP (auth) · D-UI-10 form re-impls (forms) · D-UI-14
dormant auth pair. Registered-only (<3): D-UI-12 accordion twins · print-tile pair ·
panel-copy pair · iframe-shell pair.

### 4. Scope discipline

Files touched: this log · contract Appendix A (A1 dated amendment + closure notes) · status ·
memory. Zero template/CSS/code edits; battery never (documentation sub-phase). git HEAD
`49404001` · 0 staged · 2 stashes.

**UIL-C READY:** certification total (20 family rows incl. A1 subtype, every one owner+ref
named) · queue feed final · the UIL-C session = owner SELECTS the execution queue (UI-D4) —
that selection is the next owner gate.

_Section closed 2026-07-16. Next: UIL-C consolidation queue (owner-gated)._

## UIL-C — Consolidation queue — EXECUTED 2026-07-16 · ⏸ 4 CLASSIFIED RULINGS (queue revised by feasibility evidence)

### 1. Owner UI-D4 queue selection (2026-07-16, verbatim)

> "APPROVED (safe INERT consolidation only): D-UI-01 … D-UI-02 … D-UI-03 … D-UI-05 …
> D-UI-07 … D-UI-08 … D-UI-09. Do NOT include: D-UI-04 (OTP/AUTH frozen) · D-UI-06
> (financial-adjacent) · D-UI-10 (FORMS frozen) · D-UI-14 (dormant AUTH) · any <3 clusters.
> … Stop immediately if a cluster is discovered to require architectural judgment or violates
> the inert-consolidation assumption."

### 2. Per-cluster INERT-feasibility inspection (main-thread, read-only — the §7 UIL-C plan)

| Cluster | Proposed owner | Feasibility verdict (evidence) | Risk |
|---|---|---|---|
| **D-UI-01 hero (23)** | The duplicate gradient's byte-exact owner = **`_form_styles` `.form-shell .hero`** (`135deg,#0a1628 0%,#1a2840 50%,#2a1f0e 85%,#b87333 130%` + padding 26px 30px + shadow 0 8px 32px + ::before glow) — **NOT base.html `.hero-strip`**, whose gradient/padding/shadow all DIFFER (`#1a2a4a` mid-stop, no `#2a1f0e` stop, 28px 32px, var shadow). Re-pointing to `.hero-strip` would change pixels → forbidden. | **SPLIT.** (a) files already including `_form_styles` that redundantly redeclare → pure-removal INERT ✓ (≈9 files, per-file byte-proof required). (b) whole-block byte-matches of the owner → re-point markup + remove local ✓. (c) remainder (~13 standalone files: access_control · user_dashboard · sidebar_access_list · scan_detail · roll_detail · rm_dashboard · stage_list · product_sizes_edit · product_flow · pattern_list · costing · adda_detail) share ONLY the gradient line — h1 sizes/padding/::before vary per page → **not genuinely-identical blocks; consolidating the shared VALUE needs one new token `--hero-grad-deep` (pixel-identical swap) = UI-D7 new-token owner gate** ⏸ Q-C1 | (a)/(b) low · (c) gated |
| **D-UI-02 error trio (3)** | md5-identical `<style>` ✓ — shared-include extraction mechanically trivial | **⏸ Q-C2 — documented-intent conflict:** each file carries the design note *"Kept self-contained (inline CSS, no base.html, no DB-dependent context) so it renders in any state."* Include-extraction contradicts a written design decision (include = template-loader-only dependency — technically safe in handler404/500 — but the self-containment is explicit and deliberate). Not mine to override. | judgment |
| **D-UI-03 DataTables th (3 lists / 14 sites)** | Promote the exact declaration set to ONE base.html class (§9 3+-promotion); swap the 14 `<th style=…>` sites | **EXECUTABLE ✓** — collision surface checked: base.html `table.dataTable thead th` sets ONLY `border-bottom` (!important, unaffected either way); ≤600px stacking hides `thead` entirely → mobile moot. Pixel-identical. | low |
| **D-UI-05 delete-confirm (5)** | Re-point to certified canon `.confirm-card` (`role_confirm_delete` reference) | **EXECUTABLE ✓** — content/controls/behavior preserved VERBATIM (form action, CSRF, warning text, button semantics); presentation aligns to the certified canon (the phase's visual-consistency objective; the divergent recipes are certified drift, not co-canons) | low-med (per-file control-equivalence proof) |
| **D-UI-07 status hex (18)** | `--status-*` tokens | **⏸ Q-C3 — NOT inert, value evidence:** success token `#15803d` ≠ page `#20603c`; warning `#b45309` ≠ `#a26a00`; only danger `#b91c1c` matches — and ONLY in light mode (tokens auto-flip dark to `#fca5a5`; hard-coded pages today do NOT flip). EVERY swap changes rendering somewhere. The drift is real design divergence, not mechanical duplication. | judgment |
| **D-UI-08 amber box (7)** | patterns_ai extraction | **⏸ Q-C4 — variance discovered:** only `background:#fdf7e7; border:1px solid #eadfc0` (+radius mostly) is common; paddings (10px 12px vs 12px 14px), margins (10/12/14px), font-sizes (13/14px) differ per file; `candidate_detail` = colors-only. Not genuinely-identical blocks → unification = micro visual changes. | judgment |
| **D-UI-09 filter-card (5)** | Promote `.filter-card` (+`.filter-field`, mobile stack rule) to base.html; remove the 5 page-scoped copies (barcode_dashboard · roll_list · cloth_dashboard · adda_list · adda_dashboard) | **EXECUTABLE ✓** — 5 blocks structurally identical under page scopes (byte-verify ignoring scope prefix at execution); page extras stay local | low |

### 3. Revised wave-1 definition (pending re-ruling)

**Executable now, strictly INERT:** D-UI-03 · D-UI-09 · D-UI-05 · D-UI-01(a)+(b) subset.
**⏸ Owner rulings:**

| # | Question | Recommendation |
|---|---|---|
| Q-C1 | D-UI-01(c): grant ONE new token `--hero-grad-deep` (UI-D7 gate) carrying the exact 23-file value → pixel-identical swaps kill the string at source | **GRANT** (single token, zero visual delta — the only clean total fix for the cluster) |
| Q-C2 | D-UI-02: extract shared error-style include (updating the self-containment note) vs keep-as-documented | **KEEP-AS-DOCUMENTED** (deliberate written design note; 3 small stable files; dedup value minimal vs overriding a recorded decision) |
| Q-C3 | D-UI-07: (i) full token swap accepting dark-mode alignment as intended (vocabulary law: tokens auto-flip) — changes dark rendering; (ii) swap only exact-light-matches (`#b91c1c`→danger-text), accepting its dark flip; (iii) defer the cluster | **(iii) DEFER** — every option changes rendering somewhere = outside this phase's inert license; revisit as a deliberate dark-mode-consistency FIX |
| Q-C4 | D-UI-08: unify to one spec (micro visual changes) vs keep-registered | **KEEP-REGISTERED** (per-page spacing choices = micro-redesign to unify; simplicity priority) |

### 4. Stop rationale + scope discipline

STOP per contract §16.2 (UIL-D may not start until the queue selection is settled —
feasibility evidence materially revised 4/7 approved items) + the owner's own stop
instruction ("stop immediately if a cluster… violates the inert-consolidation assumption").
Zero template/CSS/code edits this sub-phase; battery never (UIL-C docs-only; 1530/1530
stands). Files touched: this log · status · memory. git HEAD `49404001` · 0 staged · 2
stashes.

**Next: owner disposes Q-C1..Q-C4 → UIL-D wave-1 (battery-bearing) executes the settled
queue.**

## UIL-D — Wave 1 — DONE 2026-07-16 · BATTERY 1530/1530 GREEN

**Owner rulings opening the wave (verbatim):** "I accept all four recommendations. Q-C1 —
ACCEPT. Grant the single new design token: --hero-grad-deep … pixel-identical value
extraction only … Q-C2 — ACCEPT. Keep the three error pages exactly as documented …
intentional duplication, remove from the execution queue. Q-C3 — ACCEPT. Defer the entire
status-color cluster … future deliberate design-consistency phase. Q-C4 — ACCEPT. Keep the
amber-box cluster registered only. … The execution queue is now: D-UI-03 · D-UI-05 ·
D-UI-09 · D-UI-01(a) · D-UI-01(b) · D-UI-01(c) using the approved --hero-grad-deep token."
→ UI-D7 token gate RECORDED (the one new token; value = the exact extracted literal).

### 1. Executed items (serial; INERT declarations + what-each-reuses)

| Item | Change | Equivalence proof (method) | Reuses |
|---|---|---|---|
| **D-UI-01(c)** | `--hero-grad-deep` defined once in base.html `:root` (NOT dark-flipped — pages never flipped it); **21 files** had their gradient literal swapped to `var(--hero-grad-deep)` (3 spellings: spaced · compact · `var(--copper) 130%` — all value-identical; `--copper`=`#b87333`) | Value-substitution: token value byte-identical to every replaced literal → computed styles identical by construction. Counts: 0 literals remain outside base.html's (different) `.hero-strip`; 21 `var()` usages + 1 definition | the granted UI-D7 token |
| **D-UI-01(a/b)** | **Cluster arithmetic correction (census refinement):** the original "23 files" = 21 true members + base.html (grep prefix false-positive — its `.hero-strip` gradient DIFFERS at stop 2) + `costing.html` (near-miss variant `55%`, no third stop → NOT the cluster value, left untouched; sed no-op rewrite, zero content delta). Block-level dedup of local hero blocks (beyond the value) = **registered residual** for a possible later wave (per-file redundancy proofs vs `_form_styles` owner not executed in wave-1) | n/a (register) | — |
| **D-UI-09** | `.filter-card` core (container + `> *` + `.filter-field` + label) + the ≤600px stack rule **promoted to base.html**; the 5 page-scoped copies removed (barcode_dashboard · roll_list · cloth_dashboard · adda_list · adda_dashboard); page tails kept (input/fancy-select sizing; cloth's mobile variant rule with extra `padding:12px 14px` kept — it re-declares the core values + padding, rendering unchanged); 3 whitespace glue artifacts from removal fixed | Removed rules byte-identical to promoted rules (normalized core-4 hash `a025042a2853` ×5, pre-verified); tails untouched | base.html promotion (§9 3+-occurrence license; 5 files) |
| **D-UI-03** | `.dt-th` class added to base.html (exact 6-declaration set); **14/14 th sites swapped** across the 3 accounts lists (11 pure → `class="dt-th"`; 3 keep their `text-align:right` extra inline) | Declarations identical; collision surface pre-checked (dataTable th rule = border-bottom-only !important — unaffected; ≤600px hides thead). Post-counts 6/3/5 = 14, 0 residual strings | base.html promotion (3 files/14 sites) |
| **D-UI-05** | 3 accounts confirm pages REWRITTEN to the certified `.confirm-card` composition (reference `inventory/role_confirm_delete.html`); 2 storefront confirm pages: redundant inline `background:#dc2626` button overrides removed (drift → canonical `.btn.btn-danger`) | **Controls-equivalence per file:** cancel targets (user_list/skill_list/usertype_list) verbatim · POST+csrf preserved · submit labels verbatim ("Confirm Delete"/"Delete Skill"/"Delete") · body text verbatim · template blocks preserved. Presentation aligns to canon — the UIL-C-approved alignment class (NOT pixel-identical for the 3 divergent recipes; that was the cluster's stated purpose) | `.confirm-card` canon + `.btn-ghost`/`.btn-danger` |

### 2. Corpus-diff census (zero-writes-outside-allowlist)

**35 content-changed templates**, all accounted: base.html (token + 2 promotions) ·
21 tokenized · 5 filter-card · 3 lists · 5 confirm. 36th mtime = costing.html (sed no-op,
git-clean). Plus docs: UI_COMPONENTS.md (token row · `.dt-th` row · filter-card
promotion note · new Confirm-cards canon section — docs-sync same session) · this log ·
status · memory. Zero Python/JS-contract/frozen-owner changes; 0 staged; git HEAD `49404001`.

### 3. Battery (U5 — templates count as code)

Sequential fresh-DB canonical: 9-app suite **`Ran 1002 tests — OK` (168.0s)** + patterns_ai
**`Ran 528 tests — OK` (140.8s)** = **1530/1530 GREEN**. Arithmetic: entry 1530 + 0 pins
(no confirmed fixes this wave) = 1530 expected = actual ✓.

### 4. Browser evidence (dev server RESTARTED first — template-cache lesson; owner login)

12 screenshots archived (scratchpad `uil_d_shots/`, 4 pages × 360/768/1280): **user_list**
(D-UI-03 `.dt-th` headers render exact — uppercase tracked, sorting live, actions
right-aligned) · **roll_list** (D-UI-09: desktop filter row; 360px stacks full-width via
the promoted base rule; table stacks with data-labels) · **roll_detail** (D-UI-01 token hero
renders the deep navy→copper gradient exactly; supplier/cost SA-lane render intact — OWN-E
regression visual check) · **user_confirm_delete GET** (canonical card: head/body/foot,
Cancel ghost + Confirm Delete soft-danger; **no POST fired**, pk-61 untouched). Console:
**zero errors on all 4 pages**. Responsive strategy per touched page: lists = DataTables +
data-label stacking (pre-existing, re-verified) · filter bars = promoted stack rule ·
confirm cards = fixed-width card (fits 360).

### 5. Residual register (wave additions)

`costing.html` near-miss hero variant (55%, no third stop — different gradient, ×1) ·
`skill_form`/`usertype_form` user-hero variant pair (`#0f1a2e…40%/80%` — a DIFFERENT
gradient, ×2 <3) · D-UI-01(a) block-level hero dedup (post-token) · `<thead>` bg string ×3
(below cluster registration) · deferred-by-ruling: D-UI-02 (intentional duplication,
documented) · D-UI-07 (future design-consistency phase) · D-UI-08 (registered).

### 6. U6 docs-sync

UI_COMPONENTS.md updated same session (4 edits above). App GUIDE rows: N/A — zero
new/moved template files (all edits in place; the promotions live in base.html).
CHANGE_IMPACT_MATRIX: template-only edits → UI_COMPONENTS.md row satisfied; no
service/model/url rows triggered.

**Wave-1 queue arithmetic: 6 queue entries → 4 executed (03·05·09·01c) + 01(a/b)
folded-to-register (census refinement above) + 3 owner-deferred (02·07·08). Nothing silent.**

_Section closed 2026-07-16. Next: UIL-E validation (owner-gated)._

## UIL-E — Validation — DONE 2026-07-16 · ALL CHECKS PASS · CERTIFIED

Validation-only (owner order verbatim: "Do not perform any additional consolidation,
cleanup, redesign, refactoring, or implementation work."). Zero edits to any template/CSS/
code this sub-phase; battery NOT re-run (no fix wave ran — contract §7 UIL-E rule; the
wave-1 number stays authoritative, proven below).

### 1. Duplication re-census per executed cluster — EXACT

| Cluster | Expected residual | Measured | Verdict |
|---|---|---|---|
| D-UI-01 | 0 cluster-value literals outside base.html · 21 token usages · 1 definition · exactly 3 registered residual literals (costing near-miss ×1 + user-hero variant pair ×2) | 0 · 21 · 1 · 3 | ✅ |
| D-UI-09 | core blocks in pages = 1 (cloth's intended mobile variant tail) | 1, exactly that rule | ✅ |
| D-UI-03 | 0 canon th strings · 14 `.dt-th` sites · 1 definition | 0 · 14 · 1 | ✅ |
| D-UI-05 | 6 files compose `.confirm-card` (5 re-pointed + reference) · 0 old recipes (`auth-card`/`#ef4444`/`#dc2626`) in the 5 | 6 · 0 | ✅ |

No NEW variants introduced (§14 re-census guard): the only page-level `.filter-card {`
declaration left is cloth's registered variant; zero new hero/th/confirm recipes anywhere.

### 2. Wave-1 completeness + determinism

Deterministic double-render (shell, same template + context ×2): `user_confirm_delete`
sha `a7e7ace29ee3` identical ×2 · `usertype_confirm_delete` sha `7bc4d0af1d23` identical ×2
(csrf renders empty deterministically outside RequestContext — expected warning, disclosed).
**Computed-style pixel-identity proof:** browser `getComputedStyle` on the machine_form hero
= `linear-gradient(135deg, rgb(10,22,40) 0%, rgb(26,40,64)…` — the token resolves to
byte-exact original values live.

### 3. Owner-deferred clusters untouched — PROVEN

Error trio: `<style>` md5 still ONE unique hash across 403/404/500 (identical, as
documented-intentional) · D-UI-07 hex file-counts unchanged 18/30/7 (`#20603c`/`#b91c1c`/
`#a26a00`) · D-UI-08 `#fdf7e7` = 7 files unchanged. D-UI-04/06/10/14 excluded families:
no auth/forms/financial owner file in the changed-set (census §5 below).

### 4. UI-D5 accessibility baseline (pragmatic checklist — NO WCAG claim, per ratified UI-D5)

- Contrast spot-checks (computed): canon `.btn-danger` `#991b1b`/`#fee2e2` = **6.8** ·
  `.dt-th` stone-on-cream ≈ **4.1** (11px bold uppercase) · filter labels on card = **5.21**.
- Touch targets @360: filter Apply = 36px · canonical confirm `.btn` = **38px min-height —
  BELOW the `--touch-min` 44 floor. CANON-LEVEL pre-existing** (base.html `.btn` system:
  pad 9px 18px/fs 12/minH 38 — untouched by the wave; the wave aligned pages TO this canon).
  → registered UI-D5 observation **VE-1**; any fix = design-frozen canon change (owner gate),
  not wave work.
- Labelled inputs: roll_list filter = 6 labels / 8 fields (2 unlabelled — pre-existing
  markup, untouched by the CSS-only wave) → observation **VE-2**.
- Keyboard: FancySelect/Date operability = the documented known limitation (UI_COMPONENTS
  fancy-date section) — observed, unchanged.

### 5. Scope + unauthorized-file audit — CLEAN

0 `.py` modified (3h window) · 0 staged · 2 stashes · changed-template set unchanged since
the wave census (35 accounted + costing no-op) · **0 templates modified after the battery
ran → battery 1530/1530 (1002+528) remains AUTHORITATIVE without re-run** · docs-sync
markers present in UI_COMPONENTS.md (4/4: token row · `.dt-th` · filter-card promotion ·
Confirm-cards section).

### 6. Browser evidence re-check

skill_list @1280 (D-UI-03 sibling): 0 console errors, shot archived · machine_form @360
(tokenized form-shell hero): **0 console errors** + computed-gradient proof (§2) — the one
error line first seen was MY wrong-URL probe (`/machines/create/` = 404 page; real URL
`/machines/add/`; instrument error, disclosed) · prior 4-page pass: 0 errors ×4 stands.
14 screenshots total archived.

### VERDICT: UIL-E CERTIFIED — every owner-ordered check PASS; findings = 2 register-only
pre-existing observations (VE-1 canon touch-height · VE-2 filter label gaps), zero wave
regressions, zero scope expansion, zero unauthorized writes.

_Section closed 2026-07-16. Next: UIL-F library doc + certification + handoff (owner-gated)._

## UIL-F — Library doc + certification + handoff — DONE 2026-07-16 → 🏁 PHASE 10 VERDICT

### 1. Library artifact (UI-D1) — UI_COMPONENTS.md upgraded IN PLACE

Additions (no fork, no parallel doc): certification banner + **certified-canon table (19
family rows + frozen flags + reference pages)** + **deprecation register** (`.ke-toolbar`
deprecation-nominated → `.dt-filter-row`; `.filter-select` legacy; dormant auth pair
register-only) + **closed token registry** (all 127 properties by category · breakpoints vs
verification widths · standalone-set exclusions · new-token-is-owner-gated law) + frontmatter
`verified: 2026-07-16` + certified-library scope. Existing vocabulary content preserved —
the doc unifies, never forks (§7 stop test clear).

### 2. Frozen-pair reconciliation (UI-D1 cross-check) — CONSISTENT, ZERO EDITS NEEDED

`DESIGN_SYSTEM_SPEC.md` = "Permanent frontend contract — DESIGN ONLY. NO CODE." ·
`UI_COMPONENTS_CATALOG.md` = "Component reference — DESIGN ONLY, NO CODE" and ALREADY links
UI_COMPONENTS.md as "locked canon". Neither claims live-canon status; UI_COMPONENTS.md's
canonical-hierarchy section (Q-0b(1), 2026-07-13) names both as the frozen reference pair.
**No duplicate canon exists; the frozen pair stays byte-untouched** (KEEP-class). Log↔library
cross-check: canon table, deprecation register, token registry, and residuals in
UI_COMPONENTS.md match §UIL-B/§UIL-D/§UIL-E exactly (same counts, same owners).

### 3. Final duplication register (the certified split)

| Class | Clusters |
|---|---|
| **RESOLVED (wave-1, INERT-proven, battery-green)** | D-UI-01(c) hero value → `--hero-grad-deep` (21 files) · D-UI-03 th → `.dt-th` (14 sites) · D-UI-05 confirm → `.confirm-card` canon (5 pages) · D-UI-09 filter-card core → base.html (5 pages) |
| **INTENTIONALLY RETAINED (owner-ruled)** | D-UI-02 error-page trio (documented self-containment = intentional duplication, Q-C2) |
| **DEFERRED (owner-ruled, untouched-proven at UIL-E)** | D-UI-07 status-hex cluster (→ a future deliberate design-consistency phase, Q-C3) · D-UI-08 amber boxes (variance = micro-redesign to unify, Q-C4) |
| **FROZEN-FAMILY REGISTERS (gate-required, excluded from Phase 10)** | D-UI-04 OTP triplication (auth) · D-UI-10 form-shell re-implementations ~10 files (forms) · D-UI-14 dormant pre-shell auth pair · D-UI-06 expense sticky re-impls (financial-adjacent) |
| **FUTURE CANDIDATES (registered, below-threshold or post-token)** | D-UI-01(a/b) block-level hero dedup (post-token) · costing near-miss gradient ×1 · user-hero variant pair ×2 · `<thead>` bg string ×3 · D-UI-12 accordion twins ×2 · print-tile pair ×2 · panel-copy pair ×2 · iframe-shell pair ×2 · D-UI-13 micro-clusters · VE-1 canon touch-height (canon-level, design-frozen gate) · VE-2 filter label gaps · backlog #3 (trigger never fired — its template never entered the queue; stays fix-when-touched) |

### 4. §6.6 handoffs (written, record-only — NOTHING built)

| Interface | Handoff |
|---|---|
| **Phase 9 / KG (UI-D8, KOS-compat per owner permanent guidance 2026-07-16)** | PROPOSALS ONLY: (a) `component` node kind = KG-D6 additive-minor candidate — node source = the certified canon table (19 owners); edges `uses_component` (template→component, source="phase-10 census", occurrence tables in census_agents.json) + `owned_by` (component→owner file); (b) library-doc component entries + token registry = GEN-D9 target candidates (the library doc is authored fence-READY: canon table + token registry are isolatable structural sections); measured denominators on record: 169 templates · 17 families + patterns subtype · 127 tokens. Activation = dated KG/GEN amendments at their owner gates, never here. |
| **Phase 14 knowledge_sync** | Candidate detector specs (detect-and-notify law): (1) duplication re-census drift — the §UIL-E expected-residual counts (0/21/1/3 · 1 · 0/14/1 · 6/0) as recomputable invariants; (2) token-shadowing scan — hard-coded values duplicating registry tokens (baseline: 1,027 style-attrs/120 files · 897 hex/99 files, exemptions classified); (3) deprecation-usage scan (`.ke-toolbar` new-usage = WARN). |
| **Phases 19–21 deployment** | Library doc = T2 canon the runbook references; zero deploy-time behavior added (server-rendered templates only; no build step — ADR-0008-adjacent simplicity preserved). |
| **Phases 15–18 features** | Compose-from-canon now enforceable against the certified table (reference page named per family); new feature UI = owners above; new shared component = UI-D3-class gate forever. |

### 5. Queue + scope accounting (final)

**Queue arithmetic:** owner-approved 7 clusters → re-ruled at UIL-C → final queue 6 entries →
**4 executed + 1 folded-to-register (01a/b census refinement) + 3 owner-deferred (02/07/08)
— selected = done + deferred-with-reason, nothing silent, zero unaccounted items.**
**Phase-wide changed set:** docs = this log (new) · UI_COMPONENTS.md (certified library) ·
PHASE_10 Appendix A (Design Record + A1) · DOCUMENTATION_INDEX (log row + library row) ·
START_HERE (routing line) · status file · memory. Code-adjacent = base.html (token + 2
promotions) + 34 templates (wave-1, all accounted; costing = no-op). **ZERO Python/JS-
contract/migration/route/model changes phase-wide (mtime-proven at UIL-E and re-proven at
UIL-F: 0 .py in window, 0 templates post-battery). No undocumented implementation changes
exist.** Battery final: **1530/1530** (wave-1 terminal; unchanged-since proven).

### 6. Success criteria walk (§3) — 9/9

1 census-with-arithmetic ✅ (169/169; counts reconcile) · 2 canon certified ✅ (19 rows + A1,
one owner each, zero dual-owner survivors) · 3 duplication resolved-or-registered ✅ (§3
split; zero silent) · 4 behavior preservation proven ✅ (equivalence proofs per item + 14
screenshots + battery) · 5 frozen families untouched ✅ (UIL-E census + zero owner-file
changes) · 6 validation passed ✅ (UIL-E certified; responsive per touched page; UI-D5
baseline; re-census no regressions) · 7 library doc exists ✅ (upgraded in place, Standard-
compliant, routed) · 8 battery green ✅ (1530/1530 + 0 pins) · 9 handoffs + syncs ✅ (§4;
status/memory every sub-phase).

### 🏁 PHASE 10 VERDICT: **LIBRARY-CERTIFIED-WITH-REGISTERED-RESIDUALS — PHASE COMPLETE.**

The certified component library is live at UI_COMPONENTS.md (one canon, no forks); every
duplicate cluster is resolved, intentionally retained, deferred, or registered with reason;
the frozen system was never redesigned; every consolidation is INERT-proven and battery-
green. Phase 11 (DSA-0) gate input: Phase 10 CLOSED.

_Phase 10 closed 2026-07-16. Next: Phase 11 Development Dataset Architecture — DSA-0
(owner-gated)._
