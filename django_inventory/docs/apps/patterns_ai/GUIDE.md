---
id: apps-patterns-ai-guide
type: app-guide
status: active
owner: handwritten
scope: patterns_ai
anchors: config/patterns_ai/
verified: 2026-07-13
---

# patterns_ai — file-by-file guide

**State: 🔄 VISION V2 · Phase 5 AI LAYOUT OPTIMIZATION ✅ COMPLETE+FROZEN (2026-07-07; M1-M5): op:'optimize' engine (obstacles, seeded, rules-9/10 ranking) · stateless compute endpoint (zero writes) · inline optimize UI (settings/multi-select/options strip w/ Current-baseline + honest Worse-than-Current badges/preview-pauses-editing/Keep) · layout-only lightweight Undo/Redo (previews=zero entries) · E2E 17-step walkthrough proven incl. 2 M5 correctness fixes (click-never-moves + select-then-drag). Reports PHASE5_M1..M4 + PHASE5_COMPLETION_REPORT. Prior: Phase 4 WORKSPACE (2026-07-07): /patterns/workspace/<candidate>/ — infinite canvas (zoom/pan/grid/guides), drag/rotate-180-only/lock/snap, collision highlight, width+HEIGHT controls, live util/waste/length (display-only), save = NEW immutable manual candidate via save_manual_layout (verified by THE generation verifier, nest.py verify op), reload = open any candidate. 188 tests, zero migrations. Receipt: PHASE4_COMPLETION_REPORT.md + PHASE4_WORKSPACE_DESIGN.md. VISION V2 (2026-07-07, PERMANENT — see docs/AI_PATTERN_INTELLIGENCE/PRODUCT_VISION_V2.md + ROADMAP_V2.md): the product = photo→geometry→marker-layout tool ONLY. Capture/verification/generation eras = the core (DONE); usage/outcome/yield/advisor/insights = frozen research-only, never extended. Remaining: Phase 4 interactive workspace → Phase 5 locked-piece re-nest → Phase 6 PDF/print. Prior state (historical): P5 FACTORY INTEGRATION DONE (2026-07-07): Adda-detail Pattern-Intelligence links (ADR-H-safe template composition) · /patterns/insights/ executive dashboard (intelligence_service READ-ONLY, composes existing derive paths, bounded scans) · patterns_ai_health ops command · compute_bridge timeout hardening · P5_DEPLOYMENT_RUNBOOK + D7_VALIDATION_PROTOCOL (blank-until-measured) + P5_ROLLOUT_GUIDE. 176 tests, ZERO P5 migrations, pin stays 15, Django venv unchanged P1→P5. Package: P5_{DESIGN,COMPLETION_REPORT,ENGINEERING_REVIEW,REGRESSION_REPORT,FREEZE_RECOMMENDATION}.md. Prior: P4 CUT ADVISOR (2026-07-07) — one coherent assistant: /patterns/advisor/ = evidence-backed best-marker/width/ratio advice DERIVED AT READ (proven-beats-theory law, honest n/confidence, THEORY labels, already-best + no-facts honesty) + SuggestionEvent decision spine (offer snapshot frozen, ONE-SHOT human verdict; brain = advisor_service READ-ONLY test-walled; writer = suggestion_service; ZERO new models). 166 tests. Package: P4_{DESIGN,COMPLETION_REPORT,ENGINEERING_REVIEW,REGRESSION_REPORT,FREEZE_RECOMMENDATION}.md + P5_READINESS_REPORT.md. Prior: P3 GENERATION ERA (2026-07-07) — auto marker generation live: confirmed geometry → isolated engines (vendored SVGnest primary + BLF floor) → independently-verified immutable candidates → benchmark-gated HUMAN promotion (beats-baseline law) → frozen P1 library/Yield-Board machinery. 147 tests, 15-model pin. Package: P3_{COMPLETION_REPORT,ENGINEERING_REVIEW,REGRESSION_REPORT,FREEZE_RECOMMENDATION}.md + P4_READINESS_REPORT.md. Prior state: P2 GEOMETRY/CV ERA (2026-07-07) — cardboard→confirmed per-size digital truth: mat commissioning → phone capture → gated extraction (isolated compute runtime, ADR-F) → human annotator → confirm (grain+tape, trust grades) → SVG/DXF/Gate-1-print. 128 tests (13-model pin). Package: docs/AI_PATTERN_INTELLIGENCE/P2_{COMPLETION_REPORT,ENGINEERING_REVIEW,REGRESSION_REPORT,DESIGN_RECEIPT,GEOMETRY_PIPELINE}.md + ADR-D2/D3. P1 tampered-id debt CLEARED. P3 generation = owner-gated.**
Governing chain (live): [business README](../../../config/patterns_ai/README.md) → PRODUCT_VISION_V2 (🔒) → ROADMAP_V2 → PRODUCT_INTEGRATION_DESIGN (ACTIVE, owner Q-0b(2)) → ADR pack → PLATFORM_STATUS §8a. (Founding chain KICKOFF→BLUEPRINT_V3→master-plan = archived: docs/archive/patterns_ai_enterprise_era/ — line fixed 2026-07-13, Phase-7 Q-A5.)

| File | What |
|---|---|
| `apps.py` | app config; constitution note (proposals-only, zero reverse imports) |
| `urls.py` | `patterns_ai:home` placeholder (every URL needs its sidebar rule) |
| `views.py` | Block-1 placeholder view (management-gated; parse→gate→delegate) |
| `services/` | single-writer skeletons: capture · pattern_geometry · marker · marker_feedback (I-1: ALL writes incl. import/backfill enter here) |
| `templates/patterns_ai/home.html` | placeholder landing |
| `tests/test_purity.py` | source-scan wall: production never imports patterns_ai + Block-1 pins (no models, no FileFields) |
| `tests/test_smoke.py` | URL wired · 200 mgmt · 403 worker |
| `management/commands/seed_patterns_ai_sidebar.py` | idempotent SidebarItemRule seed |
| `models/` | Block 2A-2C schema: pieces (product-scoped grain) · markers (origins/status/lineage/width_band) · usage(+repeats)/outcome(FACTS-only F6) · **events (MarkerTransitionEvent — append-only, save/delete guarded)** · suggestions · mats |
| `services/units.py` | THE quantization home (I-4): int-mm facts, 2dp displays, width_band |
| `services/marker_service.py` | single writer: create (MRK- refs, lock 5376; supersede-flip) + transition machine; **emits one immutable event per status change (TRANSITIONS_VERSION)** |
| `services/marker_feedback_service.py` | single writer: usage/void (append-only) · outcome facts (N-6, null→value-only) · derive_metrics AT READ (METRICS_VERSION; never persisted) |
| `models/capture.py` | **CaptureAsset (Block 3A)**: immutable originals (identity fields save-guarded, delete refused); hash-named storage `patterns_ai/<product>/originals/<sha16>.<ext>`; dedup-per-product; stored/corrupt/retired(reason) |
| `services/capture_service.py` | single writer: store_capture (magic-byte sniff beats client lies · 1KB-15MB caps · streaming sha256 · traversal-proof naming) · retire_capture(reason) · verify_asset · mark_corrupt |
| `management/commands/verify_patterns_media.py` | ADR-G integrity sweep: sha verify → corrupt(reason); disk orphans = WARN-with-context (N-7), never corruption |
| `services/marker_query_service.py` | READ-ONLY: biography timeline · lineage · usage history · outcomes+metrics · summary (honest n) — SELECT-only, test-proven |
| `forms.py` | Block 3B: ManualMarkerForm (parse-only; ratio_text 'code:count' parser; origin/adda never exposed) |
| `views.py` | Block 3B: home + ManualMarkerCreate (ONE transaction: store_capture→create_marker; no orphan on failure) + success page — parse→gate→delegate |
| `templates/patterns_ai/manual_marker_form.html` + `_created.html` | form-shell canon, @390-first, camera capture attr |
| `templates/patterns_ai/marker_list.html` + `marker_detail.html` | Block 3C read-only library: stacked-table @390, gated thumbnails, biography/lineage panels |
| `views.py` MarkerList/MarkerDetail/MarkerThumb | pagination+ordering; detail = pure marker_query_service projection; thumb = mgmt-gated FileResponse of DERIVED rendition (never originals) |
| `templates/patterns_ai/usage_form.html` + `outcome_form.html` | Block 3D: record usage (adda scoped to product) + record outcome (metres UI → int-mm edge; all facts optional honest-NULL); detail panel 4 = usage rows w/ metrics/pending/void |
| `templates/patterns_ai/yield_board.html` + `YieldBoardView` | Block 3E: pure read model — one row/marker, avg m/100 + m/garment DERIVED AT READ via get_product_yield_board (SELECT-only test-proven); product select = only filter; 4 sorts, honest-NULLs last; best-row highlight |
| `models/geometry.py` | **P2**: GeometryExtraction (append-only, one-shot review) · PieceSizeGeometry (unique version×size, draft-mutable→frozen, trust grades, copy-forward) · PatternSetLabel |
| `models/mats.py` +CalibrationMatCheck | P2: board_spec on the mat + append-only commissioning/recheck evidence rows |
| `services/compute_bridge.py` | THE door to the isolated CV runtime (ADR-F): subprocess + JSON files, no sockets; NOT a writer |
| `services/calibration_service.py` | single writer: register/commission(tape truth)/recheck-on-capture/retire — mats never delete |
| `services/pattern_geometry_service.py` | single writer: pieces/labels · run_extraction (ADR-E hard gate, refusal=fact) · accept/reject (one-shot) · edit_draft · confirm (grain MANDATORY, tape ±2mm→MEASURED, auto-supersede) · copy-forward · DXF import/export |
| `services/svg_render.py` | READ-ONLY canonical→SVG (y-flip lives HERE only); true-scale = viewBox-parity (print-shrink pinned) |
| `services/units.py` (µm) | P2 extension: mm↔µm + validate_canonical_geometry (LOCKSTEP twin of compute runtime validator) |
| `templates/`: mat_*, piece_*, capture_form, extraction_review, version_detail, geometry_edit/print, dxf_import_form | P2 UI: registry/wizard/annotator/library/confirm/editor/Gate-1 print — canon, @390-first |
| `tests/test_p2_geometry.py` + `test_p2_views.py` | REAL-runtime goldens (±2mm tier), gate refusals, service rules, permission sweep, full browser-path flow |
| `models/generation.py` | **P3**: MarkerGenerationRun (append-only, reproducibility params w/ exact geometry_row_ids) + GeneratedMarkerCandidate (immutable placements/length/verification; promotes ONCE) |
| `services/marker_generation_service.py` | single writer P3: collect_confirmed_geometry (names missing; refuses on-fold) · start_run (isolated engines, sync+timebox) · derive_candidate_metrics (F6, no columns) · benchmark (THEORY vs recorded REALITY, width-band aware) · promote (beats-baseline LAW, evidence into creation event) |
| `compute/patterns_ai/nest.py` + `nest_runner.js` + `vendor/SVGnest/` | engines (ADR-A): BLF deterministic floor + vendored SVGnest@1248dc2 via node; INDEPENDENT shapely verification of every candidate |
| `templates/`: generate_form, generation_run, candidate_detail | P3 UI: ratio/width form · candidate comparison (derived numbers, PASS badges) · visualization + benchmark + promotion |
| `tests/test_p3_generation.py` | real-engine goldens, crafted-overlap verifier proof, benchmark gate 3 verdicts, promotion-once, P1-machinery flow-through |
| `services/advisor_service.py` | **P4 the brain — READ-ONLY (SELECT-only test-walled)**: recommend() = proven-beats-theory ranking, saving vs most-used practice, width-band advice, ratio evidence, weakest-component confidence, geometry-trust context; offer_payload() = frozen evidence snapshot |
| `services/suggestion_service.py` | single writer SuggestionEvent (I-1): record_offer (schema_version'd snapshot) + decide (ONE-SHOT human verdict; reject⇒reason) |
| `templates/patterns_ai/advisor.html` + `AdvisorView` | P4 UI: hero evidence card + alternatives (UNTESTED labels) + width/ratio panels + decision bar + append-only history |
| `tests/test_p4_advisor.py` | ranking honesty pins, SELECT-only brain, saving math, suggestion lifecycle/guards, view sweep |
| `services/intelligence_service.py` | **P5 Insights — READ-ONLY (SELECT-only test-walled)**: product saving rows (via advisor recommend), origin comparison (proven only), suggestion spine (ONE aggregate count + reality-since-decision follow-ups), bounded outcome trend, factory KPIs |
| `templates/patterns_ai/insights.html` + `InsightsView` | P5 executive dashboard: KPI chips · POTENTIAL-labeled savings · manual-vs-generated · suggestion follow-ups · trend bars · honesty footer |
| `management/commands/patterns_ai_health.py` | P5 ops truth: runtime/tools/node/media/integrity/census; exit 1 = degraded (cron/pager) |
| `production/templates/.../adda_detail.html` (3 links) | P5 workflow integration — mgmt-gated LINKS only (ADR-H: template composition like expense:/tracking:; python walls untouched) |
| `tests/test_p5_integration.py` | insights math + SELECT-only wall, suggestion follow-up derivation, health command healthy/degraded, adda-links render + boundary re-assert |
| `templates/patterns_ai/workspace.html` + `WorkspaceView` | **ROADMAP_V2 Phase 4**: vanilla-JS canvas (viewBox camera, grid/guides/snap, pointer drag, 180°-only rotate, lock, client collision highlight, live estimates); POST = save (single-writer) |
| `marker_generation_service.save_manual_layout` | Phase 4 write path: piece-multiset integrity + verify-op (same verifier) + height/width refusals → new immutable manual run+candidate (locked flags in payload) |
| `nest.py` op:"verify" | verify-only mode reusing verify_layout verbatim — one verification home |
| `tests/test_phase4_workspace.py` | verify op, save rules (overlap/height/width/set-mismatch refusals), view round-trip w/ lock persistence |
| Phase-5 optimize (nest.py op:'optimize' + optimize_layout + workspace UI) | rules 1-10: obstacle raster, effort map, invalid-first discard, util→length→compactness, stateless endpoint, strip/preview/keep, undo/redo transforms-over-base; tests test_phase5_{engine,m2_service,m3_ui} |
| `README.md` | constitution + media classes + layout |
| `compute/patterns_ai/` (repo root) | **P2 PRODUCTION isolated CV runtime (ADR-F)**: pinned venv + synth/calibrate/extract/dxf_io CLIs + artifact MANIFEST (SAM slot); see its README |
| `poc/patterns_ai/` (repo root) | P0 harness — permanent executable documentation, NON-production |
| `views.py PatternDashboardView` + `templates/patterns_ai/dashboard.html` | **PLATFORM Phase 1 (2026-07-09)**: the PATTERN DASHBOARD — owner-frozen platform entry (`patterns_ai:dashboard?product=N`); numbered workflow STEP 1 Blueprint → STEP 2 Manager → STEP 3 Digital Cutting Table (gated ≥1 Ready size, same facade law); GET-only, one facade call; production `Patterns` action redirects here (`ProductPatternsEntryView`, login-only — Blueprint keeps its strict perm at `products/<pk>/patterns/blueprint/`) |
| `models/geometry.py geometry_contract_version` (migration 0008) + `pattern_geometry_service.GEOMETRY_CONTRACT_VERSION` | Phase-1 truth stamp: every geometry row declares its semantic contract (`adr-c.1`); stamped at ALL 6 single-writer write paths (extraction accept both branches · manual edit · confirm normalize · DXF import · copy-forward CARRIES original); default backfills legacy rows |
| `tests/test_phase1_dashboard.py` | Phase-1 proof: workflow steps · DCT gate both states · chooser/403/404 · zero-GET-writes · entry redirect chain · contract stamp on every path + constant=default pin |
| `views.py PatternBlueprintView` + `templates/patterns_ai/blueprint.html` | **PLATFORM Phase 2 (2026-07-09)**: the PATTERN BLUEPRINT module (STEP 1, frozen resp. #1) — the product's structural DNA; every piece + every rule (count · required/optional · pair · fold · **grain_rule** (migration 0009, STRICT/TWO_WAY/FREE, default TWO_WAY) · fabric group) edited HERE only; atomic `+ Add Pattern Definition`; remove = refuse-if-design-history; "not registered" continuity rows for old-editor assignments; strict `change_productpattern` gate; production `product-pattern-blueprint` = redirect (D-1) |
| `pattern_geometry_service`: `set_piece_rules` · `register_pattern_definition` · `set_piece_count` · `remove_pattern_definition` | Phase-2 single-writer additions; **register_pattern_definition = the ONLY public write API for creating Pattern Definitions** (one transaction: library pattern → assignment → piece+rules). RETIRED: `PieceCreateView`/`piece-new`/`PieceCreateForm`/`piece_form.html`; Library's `set_optional` POST action (rule edits = Blueprint only; reference stays = documentation); facade rows +`grain_rule`+`fabric_group` (additive) |
| `tests/test_phase2_blueprint.py` | Phase-2 proof: atomic add (rules land, dup/blank refused, no partial rows) · register-unregistered continuity · rules/count/optional edits · bad-grain refused · remove refused-with-history / clean-removes-assignment · worker 403 + zero-GET-writes · production redirect · facade additive keys · Library lost V-1/V-2 but kept reference |
| `services/size_guards.py` + `apps.py ready()` | **PLATFORM Phase 3 (2026-07-09)**: confirmed-designs archive guard registered into production's `ARCHIVE_VALIDATORS` (dependency inversion — ADR-H wall intact); refuses archiving a size holding the product's ONLY confirmed designs (honest reason; TODO marks the future "copy Universal designs" convenience). Manager POST gains `start_universal` (explicit D-1 moment); `register_pattern_definition` auto-ensures Universal on sizeless products (same transaction). Universal = NORMAL size in every UI (owner note #1) |
| `tests/test_phase3_universal.py` | Phase-3 proof: auto-materialize+idempotent · no-auto with real sizes · reactivate-not-duplicate · button POST-only + worker 403 · normal-card render · guard refuse/allow/free cases · registered-once |
| `views.py CuttingTableShellView` + `templates/patterns_ai/cutting_table.html` + `tests/test_phase4_shell.py` | **PLATFORM Phase 4 (2026-07-09)**: DCT SHELL = the permanent workspace (owner amendments 1-7 FROZEN): toolbar order Import→Export all-disabled w/ phase tooltips + live Generate link · Pattern Library = palette-mode atoms, FABRIC GROUP→size, confirmed-of-Ready-sizes ONLY · Digital Fabric Workspace (grid+ruler+width) · Layout Information 10 permanent slots · Approved Layout Library section · 6-slot status bar · gate = ≥1 Ready else dashboard redirect w/ message · POST 405, zero writes; route table/<pk>/ = cutting-table; Manager/Dashboard gates retargeted |
| Phase 5 (2026-07-09): `cutting_table.html` interactions + `tests/test_phase5_interactions.py` | Session-only manual layout builder (ZERO writes, POST 405): [+ Import] palette affordance · single-select · mm-coordinate drag · grain-filtered rotate (strict 0°/two_way 0-180/free 4-way) · pair-gated mirror · instance delete · 60-deep undo · live Imported/Placed/Remaining + group lock · LAW-12 cross-group import refusal · selection bar + R/M/Del/Ctrl+Z; workspace payload = facade rows serialized via json_script (no geometry fork); frozen toolbar untouched |
| Phase 6A (2026-07-09): physics in `cutting_table.html` + `tests/test_phase6a_physics.py` | mm world coords FROZEN (pixels = render only; zoom/pan = camera) · runtime piece object = single truth (DOM renders) · collision PIPELINE bbox→polygon→spacing-margin (`collidePair` reusable; spacing = layer 3, profile `default_spacing_mm`) · Grid LIVE (SNAP_MM=10 internal config) · isolated `layoutMetrics()` (utilization/length — AI+approval reuse) · locked pieces selectable-but-immutable (🔒 selbar + L key; the 6B/6C manual-first constraint) · red/amber/grey paint · still ZERO writes/405 |
| Phase 6B (2026-07-09): manual optimization in `cutting_table.html` + `tests/test_phase6b_manual.py` | boundary = constraint LAYER 0 (`placementStatus` = boundary→collision→spacing, ONE pipeline for move/import/Compact/6C) · camera{x,y,scale} viewBox-only (Zoom LIVE: click/Shift/wheel/0-reset · pan = empty-drag; world mm immutable, browser-proven) · Compact = gravity assist (50→10→1mm, locks fixed, intent unchanged, improve-or-shrink-overflow rule — violating pieces walk home, no tunneling) · import = first-fit on clear fabric · nudge 1/10mm (+100 const) · col-bound violet · still ZERO writes/405 |
| Phase 6C (2026-07-09): `CuttingTableOptimizeView` (`table/<pk>/optimize/`) + client Auto Place/AI + `tests/test_phase6c_ai.py` | **PHASE 6 COMPLETE.** Auto Place = deterministic first-fit (workspace header, no search — browser-proven identical runs) ≠ AI Optimize (toolbar LIVE): STATELESS endpoint reusing `optimize_layout` verbatim (locked = `fixed` obstacles; grain → `allow_180`; strict never rotates). FROZEN CONTRACT: session in → positions out. Honest verdicts better/same/worse + tight-spacing disclosure (engine guarantees no-overlap; spacing = best-effort padding). Stage machine Draft→Optimized→(P7)Approved, human-only approval. ENGINE FRAME: polygon_mm = [ALONG-length, ACROSS-width] — client swaps both ways (`toEngineRing`/`applyPlacement`); apply keeps 2dp (int-rounding shaved 3.0mm gaps). Zero writes throughout |
| Phase 7 (2026-07-10): `models/layouts.py ApprovedLayout` (migration **0010**) + `services/layout_library_service.py` + `save_table_layout` + `tests/test_phase7_library.py` | **THE APPROVED LAYOUT LIBRARY = manufacturing truth** (owner rules 1-10): pipeline Session→Verify→Candidate→Save Draft→human Approve→ApprovedLayout→Exports. `save_table_layout` = writer EXTENSION (verbatim placements; design_key→confirmed version_id/geometry_row_id server-resolved = self-contained; LAW-12 server-side). ApprovedLayout = pointer onto immutable candidate: `layout_uid` LAY-<code>-NNNNNN (immutable ID) ≠ `version_no` (human V1/V2); supersedes chain; ACTIVE/SUPERSEDED/ARCHIVED; frozen-at-approval guards. `layout_is_stale` = DERIVED never stored (Law 11). UI: 💾 Save Draft · Approve toolbar arms post-save → dct_approve_confirm review → human POST · Export LIVE only viewing approved · library rows w/ STALE badge + View(read-only)/Duplicate/PDF/Print/Archive. ★ ProductionLayout coexists untouched (unification = P8 agenda). Endpoints table/<pk>/{save,approve/<cand>,layouts/<lid>/archive} |
| Phase 8A (2026-07-10): `ApprovedLayoutUsage` (migration **0011**) + `services/layout_usage_service.py` + `tests/test_phase8a_usage.py` | The Adda↔library CONTRACT: pointer-only row (F2 — no geometry columns, asserted by test; fabric_group = sole sanctioned denorm for the partial-unique `(adda, group) WHERE voided_at IS NULL`); F1 history-forever (delete raises; wrong pick = mandatory-reason VOID + new usage — never reassign); writer refuses non-ACTIVE/**STALE (LAW-11 enforcement point)**/cross-product/dup-group; `stamp_stage_record` once-only (8D join); derive-at-read `marker_content` (COUNT placements per size) + `expected_pieces` (× lay_count) — never stored (refinement 3). Zero production files touched |
| Phase 8B (2026-07-10): `ChooseLayoutView` (`table-usage/<adda>/`) + `services/layout_panel_provider.py` + `choose_layout.html` + `tests/test_phase8b_choose.py` | The Adda CHOOSES from the library (rule 9): current contracts + void flow (F1) + ACTIVE options (STALE disabled loud, ARCHIVED hidden, taken-group greyed); production side = `cutting_pattern.LAYOUT_PROVIDER` data-only registry (ARCHIVE_VALIDATORS inversion) + wrapped `_layout_panel` hook (provider failure → 200, section absent) + Manufacturing Layout panel on `_stage_panel_cutting_pattern.html` + adda_detail link. Zero enforcement; zero migrations |
| Phase 8C (2026-07-10): provider `content_by_pattern_size` + cutting-service marker branch + `tests/test_phase8c_expected.py` | COUNT HIERARCHY in code (owner freeze): provider = MARKER CONTENT only (per-pattern×size from stored placements; payload asserted plies-free) · production multiplies by ITS `lay_count` · expected persisted NOWHERE. `get_suggested_breakup` marker branch (content×lay÷colors, marker granularity, classic formula = untouched fallback incl. provider-failure) · `get_suggestion_source` honest label · `layout_reconciliation` ADVISORY (WARN after completion: "expected N (marker × plies), cut M. Your numbers stand" — never blocks/writes; breakdown keeps the OPERATOR's number, DB-verified) |
| **M2 (2026-07-10)**: `EvidenceItem` (migration **0012**) + `acquisition_service` + `svg_import` + `StudioView`/`StudioWorkView`/`StudioEvidenceCountView` (`studio/`, `studio/work/<piece>/<size>/`) + `studio.html`/`studio_work.html` + rail `dashboard.html` + `tests/test_m2_studio.py` | **THE PATTERN INTELLIGENCE STUDIO** (UI_WORKFLOW_FREEZE §1/§2/§4): dashboard = 5-STATION RAIL (Blueprint · Studio · DCT · Layout Library · Manufacturing, live verdicts) · BROWSE mode = the Pattern Library shelf (size tabs · creation-checklist chips = the SAME readiness law · Ready banner · conveyor "next missing piece") · WORK mode = 3-pane room (Evidence Stack → adapter → proposal (GeometryExtraction generalized: `capture` nullable + `evidence` FK) → one-shot review → draft → tape publish → conveyor advances). Adapters on ONE contract: photo (capture path, joins the stack) · DXF (runtime) · SVG (`svg_import.py`, straight segments, curves = honest refusal) · manual-dims (rectangle construct); PDF/PNG/AI = honest-unavailable menu slots (ladder rule, NO rows). Trust BY ADAPTER at accept (photo→photo_calibrated, rest→uncalibrated; tape = only path to measured). Confidence DISPLAY-ONLY (manual 100 · DXF 99 · SVG 95 · photo per-run). Compare = overlay + delta table, never auto-selects. Phone hand-off: QR (qrcode lib) → capture page `?size=` preselect + 5s stack poll. **adr-c.2 CONSCIOUS bump** (migration **0013** default): optional typed `features.notches` (validator TWINS lockstep) + editor notch mode + `edit_draft_geometry(notches=)` re-anchored + Blueprint `expected_notches`/`seam_allowance_mm` slots (`set_piece_rules`, advisory-only). Manager/size-library URLs stay live; rail no longer points at them |
| **M4 (2026-07-10)**: `ManufacturingStrategy` (migration **0014**) + `strategy_service` + `save_table_layout` plan params + `layer_multiplier`/`marker_content` (usage service) + provider plan facts + `static/patterns_ai/dct.js` + `CuttingTableRecipeView` (`table/<pk>/recipe/`) + ClothRoll +4 fields (raw_materials **0011**) + `tests/test_m4_planner.py` | **THE MANUFACTURING PLANNER** (owner plan + R1-R10): MARKER PLAN session opener (recipe · group LAW-12 · roll picker → human-confirmed usable width · lay type single/double/tubular · sizes×garments · piece selection · notes R3) persists INTO the saved layout's params (fills the ratio gap; roll_ref + recipe label = provenance) · IMPORT QUEUE (§18-c rename): required placements = ceil(Blueprint count × garments ÷ multiplier), rule chips R4 ("cuts 2/placement" on double lay), over-import refused, [Import all] + `I` key · G3 `layer_multiplier` (1/2) recorded at save, `marker_content` = placements × multiplier PER LAID LAYER (legacy no-key ⇒ 1, byte-identical), provider per-usage · size colors by CHART-ORDER INDEX (F4, numeric-chart tested), canvas tint yields to physics paint, labels `S · Name` · live estimates R2 (expected/layer · waste · ratio) · G5 nap: one-way roll ⇒ flips disabled (rotation sets narrowed + engine allow_180=false) · R5/R6 "Suggest Better Layout" = pending proposal + EXPLAINED verdict (current vs suggested · waste saved · pieces moved · Accept/Keep — never auto-applies) · §18-f DCT engine → static dct.js (frozen behaviors verbatim; conscious test updates read the file) · §18-d retirements: Generate link off toolbar, home → Dashboard-only nav, ToolRedirectView = ready→cutting-table / pieces→studio / else→blueprint (★ + workspace/generate out of NAVIGATION, pages live) · GENERICITY GUARD test = permanent no-garment-names wall (venv/tests/migrations excluded, 1-file allowlist). R10 reproducibility law recorded in plan. R8 Factory Simulation = future backlog |
| **M4.5 (2026-07-10)**: `services/fold_service.py` + adr-c.3 (migration **0015**) + `edit_draft_geometry(fold_edge=)` + editor fold mode + Studio one-shot proposals + facade `capabilities` contract + readiness-law upgrade + DCT fold placement (`dct.js`) + `LAY_FOLD_EDGES` + per-placement content math + `tests/test_m45_fold.py` | **FOLD from the cutting room** (plan §0 four facts + R2 capabilities + R3 three-concepts law: pattern owns CAN · lay owns POSSIBLE · placement owns CHOSE): **adr-c.3** typed `features.fold_edge` (straight VERTICAL outline segment, twins lockstep, pin forced conscious bump #3) · truth = the physical HALF-pattern; opened outline = DERIVED (`unfold_outline`, never stored) · **capabilities contract** = ONE derived dict on facade rows (fold=on_fold∧edge · mirror · rotation · seam · notches · count · optional), DCT payload ships `caps` replacing loose flags · readiness: blanket on-fold blocker RETIRED → confirmed+edge=READY, no edge = "mark the fold edge in the Studio" (both parity-twin bodies identically) · lay split `double_open`/`double_folded` (+tubular=2 edges; legacy double=open byte-identical), `fold_edges` in params · **placement modes** (R2-B): [On fold — opens to 1/layer]/[Normally]/[Import (unfolded)]; queue = PIECES math w/ fold-aware refusals ("place it ON THE FOLD instead") · fold placements: crease-pinned 1-D, rotation/mirror locked w/ physics hints, AUTO-MIRROR when the half's edge sits right (real bug caught in-browser), `col-fold` channel, dashed FOLD fabric line, engine sees them as fixed obstacles (zero engine change) · content math: `on_fold` placements = multiplier÷2 (marker_content + provider; legacy exact) · Studio proposals: detected-edge accept for on_fold pieces + honest expert-judgment "Is this a HALF pattern?" for unflagged (centerline auto-detect consciously rejected — would double the unfold) · Blueprint fold+pair advisory chip. DEV proof: LAY-DEV-NICKAR-000002 content {S:5} = fold-Back 1 + Body 2 + Pocket 2 |
| **M6 (2026-07-10)**: `build_layering_recommendation` (provider) + `stamp_adda_usages` (usage service) + production seams: `layering/handler.LAYOUT_PROVIDER` · `cutting/service.CUTTING_COMPLETE_LISTENERS` + gates · settings flags · layering template advisory line + `tests/test_m6_bridge.py` | **MANUFACTURING COMPLETION** (post-MANUFACTURING_INTEGRATION_REVIEW; ZERO migrations): layering edge = ADVISORY "Recommended layer length: ⟨marker length⟩ — from ⟨uid⟩ (⟨group⟩, ⟨lay⟩)… your measured length stands" via a second registry of the 8B species (wrapped; provider failure → section absent) · cutting-completion LISTENERS registry (ARCHIVE_VALIDATORS inversion) → automatic once-only usage↔stage-record STAMP (listener failure never breaks cutting) · gates `REQUIRE_APPROVED_LAYOUT` / `ENFORCE_LAYOUT_RECONCILIATION` + `LAYOUT_RECONCILIATION_TOLERANCE` — **both default OFF** (deploy→soak→owner flips; OFF = byte-identical; refusals name the choose page / both numbers; advisory WARN stays always). Browser proof = the FULL Nickar chain: fold layout chosen → 440mm advisory (operator's 0.5m stood) → suggestion Body 20·Pocket 20·fold-Back 10 (content×plies incl. fold ÷2) → cut 49 vs expected 50 WARN "your numbers stand" → 49 barcodes → usage auto-stamped to the cutting SR |
| **M8 (2026-07-10)**: `EvidenceItem.Kind.PHOTO_PLAIN` (migration **0017**, choices-only) + `acquisition_service` PLAIN_VIEWS/`_validate_plain_params`/`build_from_evidence` + `pattern_geometry_service.run_set_adapter` + `compute/patterns_ai/extract_plain.py` (bridge tool `extract_plain`) + `studio_work.html` plain-photo form · chips/thumbs · Build panel · Tape cross-checks + `tests/test_m8_capture.py` | **REAL PATTERN CAPTURE — the Evidence-SET adapter** (owner-frozen capture workflow 2026-07-10): plain photo (no mat — the REAL factory table) JOINS the set silently (params = `view` label + per-item `measurements` name→mm; owner rule: measurements belong to individual evidence items); **Build Geometry From Evidence** = ONE proposal from the whole stack — human picks the PRIMARY photo; tape `width_mm`/`height_mm` = per-axis scale TRUTH (outline bbox lands on the stated numbers exactly — scale anchored to the simplified outline); every other stated number = recorded ADVISORY cross-check (bbox diagonal derived; waist/hip = stated-only; NEVER a silent gate — the human judges vs the photo); measurement merge across the set: primary wins clashes, newest fills. CV leg: Otsu dark-object segmentation (grey table ≠ midtone — the mat `segment()` FALSE-PASSED on real photos, caught by overlay), OPEN-21 breaks tape bridges, honest gates (frame-edge touch ⇒ "retake with margin, tape BESIDE never across"; area 2–90%; stability = threshold-perturbed IoU ≥ 0.60). Trust law free: capture=None ⇒ UNCALIBRATED; publish tape gate = the one path to measured. **Browser proof = the platform's FIRST REAL GEOMETRY**: owner's actual cardboard photo → 36-vertex outline, conf 85, diagonal Δ 3.5 mm advisory → accepted → published v2 M=measured (tape-accepted) → real outline live in the DCT payload (caps intact); top_view honestly REFUSED in-UI (edge touch). OBSERVATION (pre-existing, not fixed): accept-first on a published piece opens a BARE draft (only the new size) — "Reopen in Studio" first = the copy-forward path; publishing a bare draft drops other sizes from the current confirmed version |
| **M8.1 (2026-07-10)**: `pattern_geometry_service.py` `_copy_forward_rows` + `get_or_create_draft` copy-forward + `import_dxf` replace branch + `confirm_version` backstop + `tests/test_draft_safety.py` | **BARE-DRAFT SAFETY** (owner-ordered workflow fix, readiness M8_1_DRAFT_SAFETY_READINESS.md): a published version can never accidentally lose confirmed sizes. Root cause: `get_or_create_draft` predated versioning-on-published-pieces (empty-v1 flow) — copy-forward lived only behind "Reopen in Studio", so accept-first/DXF-import opened a BARE draft (M8 discovery). Layer 1 PREVENTION: every NEW draft on a confirmed piece copy-forwards its rows (shared `_copy_forward_rows`, provenance/trust/contract carried; new pieces still bare v1; open drafts reused); `import_dxf` re-captures the carried copy in place (pre-check still refuses true double-imports). Layer 2 BACKSTOP: confirm refuses dropping an ACTIVE size covered by the current confirmed version (honest message names sizes + fix); ARCHIVED sizes exempt = deliberate retirement stays legal. Zero models/migrations/UI/ERP. 9/9 new tests; conscious fixture update test_pdm_w1 (hand-made single-size v2 draft → edit the carried row = real workflow). Browser: throwaway optional "DEV Guard" piece — publish v1 (S+M) → accept on M → draft v2 carried BOTH → publish v2 confirmed S+M, v1 superseded |
| **Audit-close (2026-07-11)**: `GeometryPrintView` tiling + `geometry_print.html` rewrite + `tests/test_gate1_tiling.py` + `tests/test_template_hygiene.py` | **FOUNDATION_V1_AUDIT items closed → 🔒 PI FROZEN**: ① Gate-1 overlay print TILES for pieces beyond one sheet — reuses `marker_tile_svg` verbatim (one-placement adapter ring, canonical y-up → page-down), same 190×277 mm window / 180×267 step / numbered C·R labels / 10 mm dashed match lines / per-sheet 10 cm bar; small pieces = 1 sheet; browser-proven on REAL Body M (4 sheets, 36-vertex outline in every window). ② permanent template-comment guard: multi-line `{# #}` opened after the first `{% block %}` = test failure (the 3×-repeat leak class); top-of-file idiom stays legal. Zero migrations |
| **M5 (2026-07-10)**: library rows context (recipe/notes/lay/roll/lineage from frozen params) + `cutting_table.html` library section (thumbnails · facts line · live search · Marker Recipes block) + `CuttingTableRecipeView` action=deactivate + `ManufacturingStrategy.LayeringType` +double_open/double_folded (migration **0016**, choices+max_length only) + `tests/test_m5_library.py` | **LAYOUT LIBRARY POLISH** (first product-engineering-mode milestone; opened w/ the one-page M5_READINESS checklist): rows show what the master recognizes — SVG thumbnail (lazy) · 📋 recipe chip · lay chip · 📝 notes · "planned on ⟨roll⟩" · "supersedes ⟨uid⟩" lineage · approved by/at; legacy pre-M4 rows default-safe (no keys ⇒ no chips, tested + T-SHIRT browser-proven) · LIVE client search over uid/name/recipe/group/lay/status w/ honest no-results line · Marker Recipes management block w/ Deactivate (soft-state, never deleted; tamper-wall 404; one fetch endpoint action). CAUGHT during browser: piecesOfGroup still read pre-caps fields ("×undefined") → caps.count/optional/mirror fix in dct.js + 2 more in-block multi-line comment leaks fixed (top-of-file multi-line {# #} = harmless, discarded by template inheritance — noted). Zero ERP files; engine untouched |
