Django==5.0.1  # pinned to installed/required version; code uses CheckConstraint(check=) (5.0 API, renamed condition= in 5.1+)
psycopg2-binary==2.9.9
django-allauth==0.61.1
python-decouple==3.8
dj-database-url==2.2.0
Pillow==10.2.0
whitenoise==6.6.0
qrcode[pil]==7.4.2
openpyxl==3.1.2    # Excel barcode exports (PR-D 2026-05-29)
reportlab==4.1.0   # PDF barcode summary exports (PR-D 2026-05-29)

# ── PD deploy-blocker bundle (2026-06-11) ──────────────────────────────
argon2-cffi==25.1.0   # PASSWORD_HASHERS[0] — was venv-only; clean deploys couldn't verify passwords
gunicorn==26.0.0      # WSGI server for production
redis==8.0.0          # backs django.core.cache RedisCache (rate limiter needs shared cache)
